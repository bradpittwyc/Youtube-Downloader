# 更新日志

## v1.28.1 — 微调版本号的行距与颜色（2026-09-13）

v1.28.0 加上版本号后，放大看发现**和标题贴得太紧、颜色也偏暗**。

原因：`.brand-text` 继承的是 `line-height: 1.35`，两行加起来比 logo（26px）还高，
所以看着挤在一起。

改法：给标题与版本号都显式指定 `line-height: 1.25`，
版本号颜色从 `--text-faint` 提到 `--text-dim`，并补 `white-space: nowrap`。

---

## v1.28.0 — 左上角显示版本号（2026-09-13）

作者要求：在左上角写上版本号。

**改法**：

- 顶栏标题「YouTube 下载器」下方加一行版本号（`v1.28.0`），
  启动时由 `app.js` 从 `api.info()` 填入 —— **不依赖任何网络请求**，立刻就有
- 小一号、灰一点，不抢标题的视线
- `title` 里带完整信息：`YouTube 下载器 v1.28.0　Electron 44.x　Node 24.x`
- **发现有新版本时**：版本号变绿并追加「· 有新版本」，点它直接打开更新弹窗
  （和顶栏那个徽标是同一个入口，两条路都能进）

---

## v1.27.0 — 自动升级的端到端验证目标版（2026-09-13）

这一版**没有任何功能改动**，存在的唯一目的是验证 v1.25.0 起内置的自动升级链路：

```
已安装 v1.26.1（带修复后的下载器）
  → 启动后自动检查，发现 v1.27.0
  → 下载 141 MB（带 sha256 校验）
  → 静默安装（/S）
  → 自动重启，版本变成 v1.27.0
```

**为什么要专门造两版来测**：升级这件事没有中间态可观察 —— 单测 `checkForUpdate()`
通过、打包启动正常、能发现新版本，都不代表「下载 + 安装 + 重启」这条链路是通的。
v1.26.1 修掉的那个下载 bug（`res.body` 不是 Node 流）就是典型例子：
前面每一步都正常，偏偏到下载才失败。

> 验证完成后这个版本**不需要保留**，可以删掉 tag 与 Release。

---

## v1.26.1 — 修掉升级下载失败（fetch 的 body 不是 Node 流）（2026-09-13）

**端到端测试抓出来的 bug。** `downloadUpdate()` 里把 `res.body` 当成 Node 流用
（`res.body.on('data', ...)`），但 Node 的 `fetch` 返回的是
**WHATWG ReadableStream**，根本没有 `.on()` —— 下载立刻失败，
而且抛出的错误信息很含糊，只看日志很难定位。

**改法**：先 `Readable.fromWeb(res.body)` 转成 Node 流。
顺带补了一条「实际收到的字节数与 content-length 不符」的校验，
避免半截文件被拿去安装。

**实测**：

```
进度 0% → 26% → 67% → 97%（125 次进度回调）
✅ 下载成功 141.1 MB，用时 48 秒，sha256 校验通过
```

> 这个 bug 只有真机跑完整流程才会暴露 —— 单测 `checkForUpdate()` 是通的，
> 打包、启动、发现新版本也都正常，唯独到「下载」这一步才失败。
> 也再次说明：**声称某个流程能用之前，必须把整条链路真跑一遍**。

---

## v1.26.0 — 自动升级的端到端验证版本（2026-09-13）

这一版没有功能改动，纯粹用来验证内置的自动升级链路：
v1.25.0（带 updater）→ 检测到 v1.26.0 → 下载 → 静默安装 → 自动重启。

---

## v1.25.0 — 自动升级（连本仓库的 GitHub Releases）（2026-09-13）

作者要求：内置 updater，连本仓库的 release，实现自动升级。

### 先解决的前提：仓库必须公开

```
私有仓库的 Release 匿名访问  →  HTTP 404    ← updater 根本读不到版本信息
改公开之后                   →  HTTP 200，附件可下载（HTTP 206）
```

**公开之前做了完整的密钥扫描**（扫全部 git 历史，不是只看当前工作区）：

```
可疑文件名      ✅ tools/probe.config.json 从未提交过
密钥特征        ⚠ 命中 1 处 → 查证是 tools/probe.config.example.json 里的
                  占位符 "sk-xxxxxxxx..."（全 x），不是真 key
                ⚠ 另一处是 ffmpeg.exe 二进制被正则误匹配
个人信息        作者名与本机路径，公开可接受
```

### 实现

新增 `src/main/updater.js`（**没有引入新依赖**）：

| 步骤 | 做法 |
|---|---|
| 检查 | `GET /repos/.../releases/latest` → 比版本号 → 取 Setup 附件 |
| 下载 | 流式下载 + 进度推送 + **sha256 校验**（GitHub API 直接给 digest） |
| 安装 | 写一段临时 `.cmd`：等 2 秒 → `安装包 /S` → `start` 拉起 App |
| 退出 | 主进程 `app.quit()`，由那段批处理接管 |

**为什么不用 `electron-updater`**：本项目从第一天起只有 **1 个运行时依赖**（docx），
这是它最值钱的工程属性。而这里要做的事并不复杂 —— 查一个公开接口、下一个文件、
跑一次安装包。为此引入 electron-updater 及其 8 个传递依赖不划算。
（代价：没有差分下载，每次要下完整的 141 MB。）

### 实测出来的两个关键事实

**① `/S` 静默升级是可行的**（真机验证）：

```
装 v1.23.0 → 运行 v1.24.0 的 Setup /S → 版本变成 v1.24.0
注册表: YouTube 下载器 v1.24.0 → D:\Program Files\YouTube Downloader
```

**② 但 `/S` 不会自动重启 App** —— 它跳过了完成页，`runAfterFinish` 不触发。
所以装完必须由 updater 自己把 App 拉起来（这就是那段临时 `.cmd` 存在的原因）。

### 界面

- **顶栏徽标**：发现有新版本时出现一个会呼吸的「有新版本」，平时完全隐藏
- **更新弹窗**：当前版本 → 最新版本、Release 说明（Markdown 原文展示）、
  下载进度条、以及一句**提前告知 UAC** 的说明
- **设置里**：显示当前版本 + 「检查更新」+「打开发布页」
- **启动后 8 秒自动静默检查一次**（延迟是为了不和启动时的索引/字体枚举抢资源）；
  失败只写日志、绝不打扰用户

### ⚠️ 会有一次 UAC 授权提示

你现有的安装是 **per-machine** 的（`HKLM` + `D:\Program Files` + `/allusers`），
替换文件需要管理员权限，所以点「下载并安装」后会弹一次系统授权。

界面上**提前写清楚了**，否则用户会以为卡住。点「是」之后就是全自动：
静默安装 → 自动重启。

> 想彻底免掉 UAC，得改用 per-user 安装（装到 `%LOCALAPPDATA%`）。
> 但那样会变成另一个安装位置、和现有安装并存，暂时不动。

---

## v1.24.0 — 模型可选 + 修掉「测试连接必然失败」与「截断白烧钱」（2026-09-13）## v1.24.0 — 模型可选 + 修掉「测试连接必然失败」与「截断白烧钱」（2026-09-13）

起因：作者问「有没有用上最新的 DeepSeek 模型」。查下来发现**两件事都不对**。

### 现状：名义上旧名，实际上早就在用新的

```
配置里写的        deepseek-chat          ← /v1/models 里已经不再列出它
服务端实际做的     静默转发到 deepseek-flash（响应里 "model":"deepseek-flash"）
这把 key 可用     deepseek-flash · deepseek-v4-pro
```

也就是说**一直在用新模型，但配置里那个名字是过期的旧别名**，随时可能彻底停用。

实测两个模型跑真实翻译（45 条字幕）：

| | deepseek-flash | deepseek-v4-pro |
|---|---|---|
| 耗时 | 6–10 秒 | **80 秒**（8 倍） |
| 推理 token | 762 | 5001（差点把 8192 打满） |

93 段的视频：flash 约 15 分钟，v4-pro 约 2 小时。**默认推荐 flash。**

### 🔴 修掉的第一个 bug：「测试连接」对推理模型必然失败

`testConnection()` 原来用 `max_tokens: 16`。**推理模型的思考 token 也算进 max_tokens** ——
16 个 token 全被思考吃光，返回空 content：

```
❌ 响应里没有 content
```

**用户点「测试连接」永远是红的，哪怕配置完全正确。** 实测两个模型都是这个下场。

改法：`16 → 512`，并且**关掉思考**（这只是一次「能不能通」的探测，不需要它思考）。

### 🔴 修掉的第二个 bug：输出被截断时会反复白烧钱

推理模型思考把预算吃满时 `finish_reason === 'length'`，JSON 从中间被截断。原代码把它
当成「格式不对」→ **重试** → 再截断 → 再重试。**每段白烧 3 次。**

改法：**先判断截断，再判断空 content**，并标记 `noRetry`：

```js
if (finish === 'length') {
  const e = new Error(`输出被截断：模型把 ${reasoningTokens} 个 token 花在「思考」上…`);
  e.code = 'TRUNCATED';
  e.noRetry = true;   // 重试只会再烧一次钱
  throw e;
}
```

### 新增：模型可选项（作者要的核心）

- 设置里「模型名」从纯手填 → **可下拉选 + 可手填**（有些兼容端点没实现 `/models`，
  不能因为拉不到就把用户卡死）
- 新增「**获取可用模型**」按钮 → `GET /models`（**免费，不消耗 token**）
- 新增 `MODEL_PROFILES` 档案表：每个已知模型带显示名、是否推理模型、**单价**、
  替代建议。不认识的模型按推理模型保守处理
- **单价跟着模型走** → 换模型时自动填 `输入价格 / 输出价格`（否则费用预估还按旧价算，
  实测切到 v4-pro 会自动变成 ￥4/￥16）

### 新增：翻译时关掉「思考」

实测（45 条字幕，同一段内容）：

```
                耗时        推理 token
开思考          10.5s          762
关思考           6.3s           —        ← 快 40%、省 27%，产出质量没差别
```

翻译是「照本宣科」的活，不需要推理。`callLLM` 加了 `reasoning: 'off'`，
**只用在逐段翻译上**；结构分析那次调用仍然开着思考（只调一次，需要规划能力）。

### 关键：改模型名**不会**让已有缓存失效

缓存键是 `videoId + srtPath`，模型名是另外单独比对的
（`if (j.model !== model) return null`）。所以直接把配置从 `deepseek-chat` 改成
`deepseek-flash`，会让 17 份已付费的缓存全部失效、白花 ￥6。

改法：新增 `canonicalModel()` **别名归一化**，把「同一个后端的不同名字」映射成同一身份，
**只用于缓存比对**，不改实际请求用的名字。

实测（缓存里写的还是旧名，配置已改成新名）：

```
缓存里记录的模型:  {"deepseek-chat": 17}
当前配置的模型:    deepseek-flash
✅ 命中缓存 × 3      结论: 3/3 命中  ✅ 改名没有让缓存失效
```

顺带加了设置迁移（`modelNameFixed` 标记）：只把「恰好等于旧默认名」的改成新名，
用户自己填的其它模型不动。

### 实测汇总

```
① 测试连接         ✅ 成功 1210ms 回复「正常」（原来必然失败）
② 模型列表         ✅ ["deepseek-flash","deepseek-v4-pro"]
③ 关思考           ✅ 结果完整 20 条 · 推理 token 开=425 → 关=0
④ 截断检测         ✅ 报 TRUNCATED 而不是格式错误，且标记不可重试
⑤ 别名归一化       ✅ 新旧名同身份 · 旧名标了替代者 · 未知模型保守处理
⑥ 缓存保住         ✅ 3/3 命中（17 份旧缓存一分没浪费）
⑦ 界面             ✅ 下拉 2 个选项 · 提示与单价联动 · 连接成功（767ms）
学习文档流水线测试   ✅ 67 条断言全绿
```

---

## v1.23.0 — 字幕默认「不折行，一行铺满可用宽度」（2026-09-13）

作者澄清：**要的不是把折行做得更好看，而是根本不要折行 —— 中英文都尽量展开**。
（v1.22.2 把折行算法修得很漂亮，但方向理解错了。）

### 改法：折行上限的默认值改成 0 = 自动

- `wrapEnChars` / `wrapZhChars` = **0 表示自动**：不设人为上限，
  一行尽量铺满可用宽度，**只有真的装不下才折行**
- `ass.js` 里改成「0 就用物理上限 `capEn` / `capZh`」，不再回退到 44 / 22
- 设置界面两个输入框的 `min` 改成 0，加 `placeholder="0 = 自动"` 与说明 tooltip
- **加了设置迁移**（`wrapAutoFixed` 标记）：老配置里存着 44 / 22（正是当时的默认值），
  不迁移的话这个改动对老用户完全无效。只认这两个旧默认值，
  用户真填过别的数值就不动

### 实测（先量后改）

全部 13 份 ASS、21826 条字幕，统计「需要折行的条数」：

```
英文（10944 条，最长 94 字符）
  上限 44 字符（旧默认）  折行 61 条  0.6%
  上限 74 字符（物理上限） 折行  6 条  0.05%

中文（10882 条，最长 34.5 字宽）
  上限 22 字宽（旧默认）  折行 56 条  0.5%
  上限 40 字宽（物理上限） 折行  0 条  0.00%   ← 一条都不用折
```

单条验证：

```
旧默认 44/22   英文 2 行「that is the motility and the pollinators」
                     + 「and a lot of other things besides」
               中文 2 行

自动 0/0       英文 1 行（宽 37）
               中文 1 行（宽 30）
```

剩下那 6 条英文是真的一行放不下（最长 94 字符 > 74），只能折 ——
折行算法仍是 v1.22.2 的「均衡 + 避头尾」，所以折出来也是两行等长、标点不跑行首。

---

## v1.22.2 — 修掉 ASS 字幕「孤字行 / 标点跑行首」的断行问题（2026-09-13）

作者反馈：生成的 ASS **有些时候换行有问题，会造成文字挤压**。

### 先把问题钉死（不靠猜，全部实测）

从 13 份真实 ASS 里量出：多行块只占 **0.5%**（118 / 21868），所以「有些时候」是对的。
而这 118 个多行块里：

```
❌ 中文第二行只剩 ≤2 字（孤字）   19 处
❌ 英文第二行只剩 1 个词（孤词）   30 处
⚠ 两行长度差 3 倍以上            76 处
```

最典型的症状（A Doctor's Gut Reset 的 0:10:59）：

```
motility（肠道动力）和 pollinators（传粉者）
。                        ← 第二行只有一个句号
```

**排查过程中否掉了三个想当然的假设**（都靠实测）：

1. ~~行太宽被 libass 再换一次行~~ —— 全量量过，**没有一行超过可用宽度**（最宽 59%）
2. ~~两行中文比预期高、顶进英文~~ —— 受控实验量出 libass 行推进**确实 = 字号**（44px），
   和代码假设一致，英文位置没算错
3. ~~英文被折成很多行~~ —— 99.4% 的英文块只有 1 行

### 真正的根因：折行算法本身

旧的 `wrapChinese` / `wrapEnglish` 都是**贪心填满**：

- 填到放不下就断 → **必然**产生「上一行塞满、下一行只剩一两个字」
- **没有中文避头尾（禁则）处理** → 宽度断点正好落在标点前时，标点被推到下一行行首
- 英文按**字符数**折行，不是显示宽度

### 改法：最小参差折行（minimum-raggedness）+ 避头尾

- `wrapBalanced()`：动态规划，代价 = Σ(行内剩余空间)² + 违禁罚分。
  总剩余空间 = 行数 × 上限 − 总宽，是个**定值**；而「和固定时平方和最小」正好发生在
  各行剩余相等时 —— 所以这个代价函数给出的就是**最均衡**的切法，天然消除孤字行
- **避头点**（`，。、；：？！）》…` 等）不得出现在行首；**避尾点**（`（《「` 等）不得出现在行尾
- 折行过程中**不切断英文单词**（中文行里的拉丁词整体算一个单元）
- DP 解不出来时（例如单个词就比整行还宽）回退到原来的贪心算法兜底

### 过程中自己写出来并修掉的两个 bug

1. **英文空格消失**：重建行时用 `join('')`，`who's like` 变成 `who'slike`。英文要 `join(' ')`
2. **英文行溢出**：DP 只累加单词宽度，**漏算了词间空格** —— 实际宽 24.5 的一行（上限 22）
   被判定成 20.5 塞进一行。改成每个词的宽度带上它前面那个空格

### 实测

全部 13 份 ASS 拿去跑前后对比（拆回原文 → 新算法重排，只算不写）：

```
中文孤字行(≤2字)    19 →  0   ✅
英文孤词行(1词)     30 →  0   ✅
两行长度差≥3倍      76 →  0   ✅
超出每行上限         0 →  0   ✅ 没有引入新溢出
```

同一帧的视觉对比（`docs/v1222-wrap-compare.png`）：

```
BEFORE  motility（肠道动力）和 pollinators（传粉者）
        。                                ← 孤零零一个句号

AFTER   motility（肠道动力）和
        pollinators（传粉者）。           ← 两行均衡，句号跟着上一行
```

`ass.js` 顺带导出了 `wrapEnglish` / `wrapChinese` / `displayWidth`，
让测试能直接验折行质量，不必绕道去解析生成出来的 ASS。

---

## v1.22.1 — 下载历史也加翻页（每页 20 个）（2026-09-13）

作者要求：历史列表也要翻页，每页只显示 20 个。

**改法**（和队列翻页同一套做法，保持一致性）：

- 新增 `HISTORY_PAGE_SIZE = 20` 与 `historyPage`
- 弹窗底部加翻页条（右侧），显示 `21–36 / 36　第 2/2 页`，首页/末页按钮置灰
- **只有一页时整条隐藏**，和队列翻页的行为一致
- **换筛选条件时回到第 1 页** —— 否则会停在一个不存在的页上（例如从 36 条筛到 18 条时，
  原页码 2 已经越界，夹取会把用户莫名其妙地甩到第 1 页，不如显式归零）
- 固定 20 条而不是「按窗口高度算」——理由同队列：窗口缩放时条目不会跳来跳去

**实测**（临时在下载目录造 25 条假历史凑到 36 条，验完已删除）：

```
总数          共 36 个（11 条真实 + 25 条测试）
第 1 页       20 条，"1–20 / 36　第 1/2 页"，上一页置灰
第 2 页       16 条，"21–36 / 36　第 2/2 页"，下一页置灰，与第 1 页不重叠
筛选 OpenAI   18 / 36 个 → 单页 → 翻页条正确隐藏
清空筛选      回到 "1–20 / 36　第 1/2 页"
```

测试数据清理后，磁盘上的真实历史仍为 11 条。

---

## v1.22.0 — 新增「下载历史」（2026-09-13）

作者要求：队列底部**左边**加一个「下载历史」按钮（右边仍是翻页），
呈现过往所有下载；并且**「清除已完成」只清当前队列显示，不清空历史**。

### 好消息：历史本来就已经存在，只是看不见

每个下载过的视频旁边都有一个隐藏的 `<视频名>.video.json`（边车文件），
里面存着 `id / title / channel / uploadDate / duration / url / at(下载时间)`。
它的原始用途就是「**不依赖队列历史**地识别本地已有作品」——
换机器、重装、把队列清空，重新扫一遍下载目录就能全部还原。

所以这次**没有新建任何存储**，只是把已有的数据呈现出来。

`queue.clear('done')` 本来也只删队列里的条目、不碰磁盘 ——
「清除已完成不清空历史」这条其实早已成立，只是用户看不到历史、无法确认。
现在能看见了，也在按钮的 tooltip 里写清楚了。

### 改法

- `downloads:index` 这个 IPC **多透出 4 个字段**（`channel` / `url` / `at` / `file`）——
  边车里本来就有，之前没传出来，导致历史列表只能显示标题、分不清是谁的
- 队列底部新增 `.queue-foot`：**左边「下载历史」，右边翻页**
  （用 `space-between` 而不是 `flex-end` —— 翻页条只有一页时会整条隐藏，
  这样历史按钮会自动落到左边，位置始终稳定）
- 新增历史弹窗：按**下载时间倒序**，每条显示 标题 / 频道 / 时长 / 发布时间 /
  **下载时间** + 「打开」按钮；顶部有筛选框与「重新扫描」
- ⚠️ 历史入口**不能**放进翻页条里 —— 只有一页时翻页条整条隐藏，入口会跟着消失
- 「清除已完成」的 tooltip 改成：*只把已完成的任务从队列里移走，文件仍在磁盘上，
  「下载历史」里的记录也一条都不会少*

### 实测

```
历史条数        11 条（磁盘扫描）
前三条          标题 + "📺 OpenAI ⏱ 3:48 发布于 2026-09-09 下载于 2026-09-13 08:52"
筛选"OpenAI"    6 / 11 个；清空筛选回到 11 个
清除已完成之后   历史仍为 11 条，「历史未受影响」= true      ← 核心要求
单页时          翻页条隐藏、历史按钮仍可见
位置            底部栏 904→1304，历史按钮左缘 916（贴左）、翻页右缘 1304（贴右）
```

> **过程中弄脏了一次数据**：上一轮被中断的验证脚本点了「清除已完成」，
> 把我备份的真实队列还原回去了（10 条）。文件与历史都没受影响 ——
> 但这也说明**验证脚本会动真实数据，跑之前一定要备份**。

---

## v1.21.1 — 下载队列改成翻页（每页固定 8 条）（2026-09-13）

作者要求：队列区块固定只显示 8 条，翻页控件放在区块**右下角**。

**改法**：

- 新增 `QUEUE_PAGE_SIZE = 8` 与 `state.queuePage`
- `renderQueue()` 只渲染当前页的 8 条；不在当前页的 DOM 节点会被原来的「清理未使用节点」
  逻辑回收，所以**每页也不会越积越多**
- 翻页条贴在队列区块右下角，显示 `9–16 / 20　第 2/3 页`，首页/末页时对应按钮置灰
- **只有一页时整条隐藏** —— 没有可翻的，摆着只是噪音

**两个必须处理的细节**：

1. **固定 8 条，而不是「按窗口高度算能放几条」**：窗口大小变化时条目不会跳来跳去，
   翻页的含义也始终稳定（第 2 页永远是第 9~16 条）。
2. **加完任务翻到第 1 页**。队列是按状态分组的（下载中 → 等待 → 暂停 → 失败 → 完成），
   刚加入的任务排在「下载中」之后，几乎总在第 1 页里 —— 点完「开始下载」立刻能看到它。

页码越界（条目被删光、清空队列）时 `queuePageItems()` 会自动夹回合法范围。

### 过程中发现并修掉一个自己写出来的 bug

第一版想「**自动跳到新任务所在的那一页**」，做法是对比前后列表、找第一个没见过的 key。
实测**页码会自己跳**：`1 → 2 → 1`（启动后 5 秒内跑完），因为中途只要来一份条目较少的快照，
`queuePageItems()` 里的夹取就会把页码压回第 1 页。

这种「靠推断猜哪些是新条目」的做法本身就脆，改成**不猜**：只有用户自己点「开始下载」
时才在 `enqueue()` 里翻页，`onChanged` 只负责渲染。

**实测**（临时造 20 条队列，验完已还原）：

```
页码稳定性   20 秒内 sequence = ["1–8 / 20 第 1/3 页"]，纹丝不动        ✅
翻页正确性   第 2 页 9–16/20 共 8 条 · 末页 17–20/20 共 4 条 · 末页按钮置灰 ✅
加任务跳页   走真实 UI（识别频道 → 点行内「下载」）：
             点下载前 17–21/21 第 3/3 页 → 点下载后 1–8/22 第 1/3 页，
             新任务可见                                                 ✅
单页隐藏     7 条的真实队列下翻页条正确隐藏                              ✅
```

> **顺带记一笔**：写验证脚本时用了 `① ② ③` 做属性名，它们**不是合法的 JS 标识符字符**，
> 脚本直接 `SyntaxError` —— 但报错位置指向 `index.html`，很容易误以为是应用的错。
> 教训：验证脚本也要先 `node --check`。

---

## v1.21.0 — 给「最贵的那条链路」补上端到端测试（2026-09-12）

### 问题：测了便宜稳定的部分，没测昂贵脆弱的部分

`tools/integration-test.js`（70+ 断言）覆盖的是「识别 → 下载 → 断点续传」——
那些是 **yt-dlp 在干活**，便宜且稳定。而**学习文档流水线**
（608 行 pipeline + 468 行 index + LLM 调用 + Word/ASS 排版）**一行测试都没有**。

文件里甚至写着一句：

```js
// 集成测试不触发大模型（学习文档由 tools/study-cli.js 单独验证）
```

但 `study-cli.js` 是**要 API Key 的手工探针，不是自动化测试**，挡不住回归。

**代价已经付过一次**：`validatePlan` 传错对象 + `takeaways/quotes` 引用未声明变量
这两个 bug，让**任何没有翻译缓存的新视频都生不出文档**，跨了好几个版本没人发现 ——
因为已缓存的视频走的是另一条旁路，表面上一切正常。最后是作者实际用时才撞出来。

### 改法：`tools/study-test.js`（65 项断言，几秒跑完，不联网、无费用）

把 `llm.callLLM` 换成**可编程的假模型**：

- **结构分析**：按提示词里的【总条数】造一个合法计划（topic 带「模型分段」前缀，
  以便区分「模型给的」和「兜底均分的」）
- **逐段翻译**：从用户消息里**解析出真实序号再原样回显**，保证 `validateCues` 一定通过

于是不需要 API Key，也不产生任何费用，可以随时跑。覆盖：

| 分组 | 内容 |
|---|---|
| A 结构分析 | 正常计划 / 结尾少算被自动修复 / 计划完全不可用走兜底 / 坏 JSON 重试 / 越界重叠乱序被修复 |
| B 翻译与汇总 | 逐条中文 100% 覆盖 / **takeaways 与 quotes 非空** / 词汇去重 / 每段四要素齐全 / 时间码单调 / 失败段落被隔离 |
| C 断点续跑 | onPartial 回调 / 复用已完成段 / **只翻缺的段且调用次数精确** / 字幕变了拒绝复用 |
| D 产出物 | docx 段落数 == 段数、含 takeaways/金句/词汇表；ASS 事件数 == 条数×2 且含中文、描边为黑、PlayRes 一致；双语 SRT 条数正确 |
| E 编排 | 三产物落盘 / SRT 带 BOM / 二次调用命中缓存 0 次模型调用 / 金句卡片自动渲染且失败不连累文档 / force 重跑 / 中间结果跑完被清理 |

docx 的校验**手写了一个最小 ZIP 读取**（走中央目录 + `zlib.inflateRawSync`），
不引新依赖就拿到了 `word/document.xml` 的真实文本。

### 关键：做了变异测试，确认测试真的抓得住

**只会通过的测试没有价值。** 把两个 bug 故意放回去各跑一次：

```
注入 bug ①（validatePlan 传整个对象）→ 退出码 1，5 条失败：
   ❌ A1 段落主题来自模型 — 首段主题=「第 1 部分」（模型计划被丢弃、退化成兜底均分）
   ❌ A1 没有走兜底均分 — usage.fallbackPlan = 1
   ❌ B2 takeaways 非空
   ❌ B3 quotes 非空
   ❌ A4 坏 JSON 后重试成功 — 调用 3 次

注入 bug ②（引用未声明的 takeaways / quotes）→ 退出码 1：
   测试本身崩了： ReferenceError: takeaways is not defined
```

**第一版测试还有个弱点**：只断言「跑通了」时，bug ① 注入后 A1 竟然还通过 ——
因为流水线悄悄退化成兜底均分，文档照样生成得出来。
所以补了两条更直接的断言（主题必须来自模型、`usage.fallbackPlan` 未置位），
这才让变异测试变得决定性。

### 顺带发现（本次未修，属于功能缺口）

**学习文档没有取消机制**：`queue.cancelAll()` 只杀下载的 yt-dlp 子进程，
完全不管 `item.studying` —— 点了「全部取消」，正在跑的翻译不会停，
会继续在后台烧钱直到跑完。

### 脚本

```bash
npm run test:study        # 新增：几秒、离线、无费用 —— 日常改动优先跑
npm run test:integration  # 原有：真实联网，5 分钟以上
npm test                  # 两个都跑
```

---

## v1.20.4 — 频道列表里已下载的视频加「打开」按钮（2026-09-12）

作者要求：在**已下载**的视频那一行、「下载」按钮的**左边**加一个「打开」，
点开就是下载后的文件夹（和下载队列里的「打开」行为一致）。

**改法**：

- 新增 `localPathOf(id)`：查这个作品在磁盘上的位置。**两个来源，优先队列**
  （最新最准），回退**磁盘边车索引** —— 队列被「清除已完成」清掉之后，
  仍然能靠边车文件找到它。
- 列表行渲染：`已下载` 的行在「下载」左边插入「打开」按钮；
  没下载的行不显示（避免给出一个点了没反应的按钮）。
- 点击后调 `shell:open-path`。该 IPC 本身就会**自动取父目录**再打开，
  所以传文件路径进去，弹出的就是它所在的文件夹。
- 找不到文件时提示「找不到这个视频的本地文件（可能已被移动或删除）」，
  而不是静默什么都不做。

---

## v1.20.3 — 去掉「已加入队列」提示；打包加防误杀保险（2026-09-12）

### ① 去掉「已加入队列」提示

作者指出这个提示不用显示：

```
已加入队列 1 个（将同时下载音频 + 字幕 + 学习文档）
```

点完「开始下载」之后，队列面板会自动滚进视野、条目当场就出现了 ——
再弹一个 toast 重复同一件事纯属噪音。

**改法**：移除该 toast。但保留一个例外 —— **真的跳过了队列里的重复项时仍提示一句**
（「已在队列中的 N 个已跳过」），那是用户从界面上看不到的信息。

其余「点击确认」类的提示（已清除已完成任务 / 设置已保存 / 已隐藏 N 个已下载的视频 等）
保持原样，等作者确认是否也要去掉。

### ② 打包流程加保险：绝不再杀掉正在跑的任务

**起因**：这一版打包时检查出「有 1 个任务正在跑」，但那只是一句 print，
**没拦住流程** —— 于是又把一个刚跑到 10/11 段的翻译打断了。
这是我第二次犯同一个错（第一次害得学习文档卡在 18/93）。

**改法**：新增 `tools/safe-build.js`，`npm run dist` 改为先跑它：

- 队列里有任务在跑 → **直接拒绝打包**（退出码 1），并列出是哪些任务、跑到哪了
- **但同时检查 App 到底有没有在运行** —— 队列里的 `studying` / `downloading`
  可能只是上次被杀留下的过期标记（App 一重启就会被 `load()` 里的自愈逻辑复位），
  光看字段会误判成「正在跑」而白白拦住。只要 App 没在运行，就不可能有任务真的在跑

**实测**：造一个「正在跑」的队列 → 退出码 1、正确列出任务；
App 未运行时不误拦 → 退出码 0。

**这次的损失**：那个视频的**完整翻译缓存已经写好了**（34 KB），被杀时只是在写
文档/字幕/金句图，**不花钱**，重跑一次即可。

---

## v1.20.2 — 描边默认值定为 1（2026-09-12）

作者把描边从 0 到 5 各档都试过一遍后，认定 **1 最好**，要求把软件的默认值也定成 1。

改动两处（保持一致，避免以后有人改了默认值却忘了兜底值）：

- `settings.js` 的 `assOutlineWidth`：3 → **1**
- `ass.js` 的 `BASE.outline`（设置没给或给了负数时的兜底）：3 → **1**

**等比缩放依然成立**，各分辨率的描边/字号比例都是 2%：

```
2160p → 描边 2.0  字号 100
1440p → 描边 1.3  字号  67
1080p → 描边 1.0  字号  50
 720p → 描边 0.7  字号  33
 360p → 描边 0.3  字号  17
```

所以大屏小屏看起来的粗细是一致的，不会出现「换台设备就变样」。

已按 1 重新生成全部 5 个视频的 ASS（命中翻译缓存，零 API 花费）。
**只影响 ASS 字幕**；Word 文档与金句卡片不受这个设置影响。

---

## v1.20.1 — 边车 json 改为隐藏文件；描边偏细的排查（2026-09-12）

### ① 「为什么会多生成一个 json 文件」

那个是 `<视频名>.video.json`（约 300~460 字节），**是设计如此、不是多余的**：

文件名模板是「标题 [日期].mp4」，**不含视频 ID**，所以光看文件名无法判断某个作品
是否已经下载过。边车把 ID 落在文件旁边，扫目录即可还原全部记录 ——
「已下」角标、「仅显示未下载」筛选都靠它；队列被「清除已完成」删掉、
或换台机器重装后，也还能凭它恢复。顺带它还是作品详情的本地缓存。

但它和视频并排躺在同一个文件夹里，看着确实碍眼。**改法**：写入时用 `attrib +h`
加上 Windows 的「隐藏」属性（老边车在扫盘时补一次）。资源管理器默认不显示，
扫盘照旧能读到，**功能一点不受影响**。

**实测**：6 个边车的属性都变成 `Hidden, Archive`，不加 `-Force` 已经看不到。

### ② ASS 描边很丑，不是一条黑色

查下来是**两个因素叠加**：

**a) 设置里 `assOutlineWidth = 2`**（应用默认是 3，这份被改成了 2）。
代码算得没错：2160p 视频得出 4.0、1080p 得出 2.0、360p 得出 1.0，等比缩放是对的。

**b) 英文用的是衬线体 Times New Roman，笔画极细**，描边一裹就发毛 ——
这是「很丑」的主因。

做了对照渲染（字体 × 粗细共 7 种组合，3 倍放大逐像素看）：

| 组合 | 效果 |
|---|---|
| Times New Roman @2（原设置） | 细灰虚边，最差 |
| Times New Roman @3 | 好些，但细衬线仍被描边吃掉、发毛 |
| **Arial @3** | **一条干净的黑色描边，最佳** |
| Arial @4 | 很粗很醒目 |

本次先把**描边改回 3** 并重新生成全部 5 个视频的 ASS（命中翻译缓存，零 API 花费）。
字体是否换成无衬线体交给作者决定 —— 那是审美选择。

### 顺带验证

重新生成时我用的 Node 测试环境没有 `BrowserWindow`，金句卡片渲染报错，
但**文档 / ASS / SRT 照常生成** —— 说明给卡片加的 try/catch 是必要的、也是对的。

---

## v1.20.0 — 学习文档断点续跑 + 金句图自动生成（2026-09-12）

### ① 断点续跑：中途被打断不再白花钱

**问题**：逐段翻译是整个流水线最贵的一步（一个 93 段的视频约 ¥0.68、十几分钟），
而中间结果**只在全部跑完之后才写缓存**。进程中途被杀（关 App、打包、系统更新）
就把已经翻好的段落全丢了，下次从 0 重来、重新花钱。

**改法**：

- `runStudyPipeline` 新增 `onPartial` 回调，**每翻完一段**就把进度交给调用方
- `index.js` 把它落成 `<缓存键>.partial.json`（写入节流 1.5 秒一次，
  但最后一段必写；跑完后再删掉）
- 重启后 `readPartial` 找回中间结果，连同**同一份分段计划**一起复用，
  只翻缺的那些段

**两个关键细节**：

1. 中间结果里存的是**最终形态的分段计划**（已经过句末吸附与大段拆分），
   所以续跑时必须**跳过这两个变换** —— 再变换一次会得到完全不同的段落划分，
   已翻好的段落就对不上了
2. 中间文件只存必要字段（en/zh/notes/vocab/cues），
   `_seg`/`_cues`/`_expected` 在续跑时重建，文件小得多

**实测**（限 400 条字幕，控制成本）：

```
第一轮：跑到 2/13 段时模拟被杀，中间结果落盘 14 KB
第二轮：复用 2 段，只翻剩下的 11 段
        逐条中文 400/400 覆盖，失败段落 0
```

**顺带修掉一个 bug**：续跑时 `done` 已初始化为 `resumed.size`，
而跳过已完成段时又自增了一次，界面上会出现「15/14」「20/14」这种超出总数的进度。

### ② 金句图自动生成，去掉「金句图」按钮

以前要在队列里点「金句图」按钮才渲染。现在**生成文档时顺手产出**：
文档里既然已经有金句，直接渲染成图片即可，不用再按一次。

- 新增步骤：`write 渲染金句卡片`，输出到视频旁边的 `<视频名>.金句卡片/`
- 卡片渲染依赖 Electron 窗口，所以**单独 try/catch** —— 万一失败也绝不连累文档
- 队列阶段文字会显示「已完成（视频 + 音频 + 2 个字幕 + 双语ASS + 学习文档 + 6 张金句图）」
- 移除界面按钮、`study:quote-cards` IPC、preload 入口，以及只服务于它的 `readQuotesFor`

**实测**（缓存命中的视频，零 API 花费）：

```
[0.0s] cache 命中翻译缓存    [0.2s] write 渲染金句卡片
耗时 2.8 秒 · 卡片数 6 · 6 张 PNG 都有实际内容（197–272 KB）
```

**测试踩的坑**：独立 Electron 脚本默认 userData 是 `%APPDATA%\Electron`，
而真实 App 用 `%APPDATA%\youtube-downloader` —— 不 `app.setPath` 纠正的话
翻译缓存全部读不到，测试会傻乎乎地重新翻译整篇（实测烧掉过一次）。

---

## v1.19.4 — 双语 SRT 默认改为开启（v1.19.3 的迁移有漏洞，等于没修）（2026-09-12）

v1.19.3 以为把 `studyBilingualSrt` 改回 true 就完事了，但实测设置里**仍是 false**、
新生成的视频**依然只有 ASS 没有 SRT**。两个原因：

**① 默认值本身就是 `false`**

```js
studyAss: true,
studyBilingualSrt: false,   // ← 早期为了"避免播放器挑 srt 看不到颜色"设的
```

那个取舍后来被证明是错的（Windows 自带的 Media Player 不支持 ASS），但默认值没跟着改。

**② 迁移只判断了 `=== false`**

```js
if (disk.studyBilingualSrt === false) { ... 改成 true ... }
disk.bilingualSrtFixed = true;   // ← 标记无条件打上
```

而**老配置里往往根本没有这个字段** —— 内层条件不成立、标记却被打上，
于是永远不再修正。这正是作者机器上发生的情况。

### 改法

- **默认值改成 `true`**，并把原因写进注释
- **迁移条件改成 `!== true`** —— 字段缺失与显式 `false` 都能修正；
  标记语义不变（之后用户手动关掉不会再被覆盖）

**实测**：造「字段缺失」的配置，load 后内存与磁盘都变成 true ✅

### 顺带

给作者已生成的 Jordan Peterson 视频补出双语 SRT（命中缓存，零花费），
并把设置改回 true。现在 **4 个视频都同时有 ASS 与 SRT**。

---

## v1.19.3 — 修复「翻译卡在 18/93 段」（2026-09-12）

作者反馈：学习文档一直卡在「翻译 18/93 段」不动。

### 排查

```
主进程 CPU 增长 = 0        ← 完全没在干活
到 API 的连接   = 无        ← 没有请求在等
studying        = true      ← 但队列里仍标记为「正在跑」
```

**任务其实早就死了，是界面在说谎。**

### 为什么会死

我打 v1.19.2 包时执行了 `Stop-Process` 杀掉 App，
当时这个视频正翻译到 18/93。**责任在我。**

但程序也有 bug：重启后队列把 `studying: true` 原样恢复回来，
**既不报错也不结束**，用户根本分不清是卡住了还是慢 —— 这一卡可以卡到天荒地老。

### 改法

`queue.js` 的 load 自愈段里加一条：上次退出时若还在跑「下载后处理」
（`studying` / `extractingAudio` / `fetchingSubs`），一律复位为 `false`，
并把阶段文字改成：

```
已完成 · 上次生成文档中断在「翻译 18/93 段」，可点「生成文档」重新开始
```

这三件事全在内存里进行、且不写中间结果，**绝不可能跨进程存活**，所以复位是安全的。
**不自动重跑** —— 那是花钱的操作，必须用户自己点。

单元验证：造一个 `studying:true` + 冻结阶段的队列文件，
load 后 `studying` 变 `false`、阶段文字带「中断」说明，正常完成的任务不受影响。

### 遗留（待确认）

学习文档流水线**没有断点续跑** —— 进程被杀会丢掉已翻好的段落
（这次丢了 18/93 段，约 ¥0.13）。要做续跑需要在每段完成后落一份中间缓存、
并复用同一份分段计划，改动较大，先问作者要不要。

---

## v1.19.2 — 修复「中文字幕完全看不到」（2026-09-12）

作者反馈：ASS 字幕「根本不是中文，而且渲染好像有问题」。

### 排查结论：ASS 文件本身完全正确

- 3 个 ASS 文件**都含中文**（2749/5507、2574/5150、72/144 条，与英文成对）
- 用 ffmpeg + libass 逐帧渲染验证：中英颜色、黑描边、位置全部正常
- PlayRes 与视频实际分辨率一致
- 放大逐像素看：中英**没有真正重叠**（英文下伸部没碰到中文）

真正的原因是**两件事叠加**：

**① Windows 11 自带的 Media Player 不支持 ASS，只认 SRT**

作者机器上没装任何支持 ASS 的播放器（PotPlayer / VLC / mpv 全都没有），
`.mp4` 默认关联到 Media Player。实测打开视频，**画面里一个字幕都没有**。

**② 程序自己把双语 SRT 关掉了**

`settings.js` 里有一条早期的一次性迁移：

```js
// 升级到 ASS 双语字幕前保存的设置里没有 studyAss 字段
if (disk.studyAss === undefined && disk.studyBilingualSrt === true) {
  disk.studyBilingualSrt = false;   // 假设用户用「支持 ASS 的播放器」
}
```

理由是「.srt 与 .ass 并存时播放器可能挑 srt，看不到颜色」。
**这个假设在作者的机器上不成立** —— ASS 读不了、双语 SRT 又被关了，
于是中文字幕彻底消失，只剩 yt-dlp 下的 `.en.srt`（纯英文）。

### 改法

1. **撤销那条迁移**，改成反向迁移：把被自动关掉的 `studyBilingualSrt` 重新打开，
   用 `bilingualSrtFixed` 做标记 —— 之后用户手动关掉不会再被覆盖
2. **界面说明改写清楚**：ASS 有颜色但需要支持的播放器，
   **Windows 自带的读不了**；建议两个都生成，播放器里手动切轨
3. **双语 SRT 改成带 BOM 的 UTF-8** —— SRT 没有声明编码的地方，
   Windows 自带播放器遇到无 BOM 的 UTF-8 常按 ANSI 解析，中文整片乱码
4. 给作者已有的视频**补出双语 SRT**（用缓存，零 API 花费），共 3 份

### 另记一笔（未改动）

ASS 的默认行距 `lineGap = -0.2` 在 640×360 这种小尺寸下，
比 SRT 的自然行距明显更紧（两组渲染对比可见）。该值是早先按 1080p 调的，
是否放宽待确认。

---

## v1.19.1 — 去掉文档目录、词汇表去掉「段数」列、表头居中（2026-09-12）

作者看了生成的文档后提的两点：

1. **学习文档不需要目录** —— 把 v1.19.0 加的「本节目录」表整个删掉
   （连带清掉只服务于它的 `TocCell` 样式）。文档从 **178 页减到 112 页**。
2. **词汇表的「段数」列没用** —— 删掉，把宽度分给词汇和释义列，
   避免音标列被挤到断行（截图里「段数」两个字竖着断成两行，很难看）。

另外把表头「词汇 / 音标 / 词性 / 释义」改成**居中**。
正文里短列（音标、词性）也居中，长文本列（词汇、释义）保持左对齐 ——
长文本居中会参差不齐，反而更难读。

**实测**：Word 打开逐项核对 —— 表头四个单元格全部 `center` 对齐；
`document.xml` 里「段数」出现 **0 次**；表格数从 2 减到 1；目录表已不存在。
已用缓存重新生成作者那份文档（零 API 花费）。

---

## v1.19.0 — 修好学习文档（两个致命 bug）+ 提示词与排版精校（2026-09-12）

### 起因

你装完第一次下载就发现：**文档和 ASS 都没生成**。查下来主流水线有两个致命 bug，
**任何没有翻译缓存的新视频都必然失败**。

### bug ① `validatePlan` 校验的是错的东西

```js
// pipeline.js（旧）
const candidate = llm.parseJsonLoose(r.content);
if (llm.validatePlan(candidate, work.length).length === 0) plan = candidate;
```

`validatePlan` 第一句是 `if (!Array.isArray(plan)) return ['分段计划不是非空数组']`，
而提示词要求模型返回的是**对象** `{plan, takeaways, quotes}` —— 于是**永远判定不合法**。
两次重试用的是完全相同的输入，自然得到完全相同的结果，于是抛「结构分析失败」。

项目里明明有 `extractStruct()` 专门拆这个对象，**但只在补总结的旁路里用了**，主流水线从没调用。

**实测证据**（那个 2758 条字幕的真实视频）：

```
旧逻辑：validatePlan(整个返回对象) → "分段计划不是非空数组" → ❌ 必然失败
新逻辑：extractStruct 拆出 plan  → 31 段 → validatePlan → 0 错误 → ✅
```

模型这次给的计划**本身完全合法**，纯粹是我们在校验错的对象。

### bug ② 返回值引用了从未声明的变量

```js
return {
  vocab: vocabAgg,
  takeaways,                              // ← 从未声明
  quotes: resolveQuoteTimes(quotes, work), // ← 从未声明
```

严格模式下这是 `ReferenceError`。所以即使修好 ①，走到最后一步照样炸。

**为什么以前"看着正常"**：已缓存的视频走的是另一条旁路（`runSummaryOnly`），
那里正确用了 `extractStruct`。缓存里的 `promptVersion=4` 记录全是旁路补出来的 ——
**自 v4 以来主流水线一次都没跑通过**。

### bug ③ 顺带修：结构分析输入里没有总条数

模型得自己从最后一行数出总数，而长字幕下这几乎必然出错
（实测 2758 条要喂 639 行）。现在把「总条数」明确写进用户消息，
并强调「序号是字幕条目编号，不是行号」。

### 加固

- **`repairPlan()`**：模型给的边界有偏差时自动修复（排序 → 起点接上一段吸收空洞与重叠 →
  裁到 [1,total] → 结尾拉长），不再因为差几条就整单失败。
  13 种畸形输入全部覆盖测试通过。
- **重试带上反馈**：把「上一次哪里不合格」告诉模型，而不是原样重发。
- **兜底分段**：模型完全给不出计划时按条数均分（切在句末），宁可分段粗糙也不要生不出文档。

### 提示词精校

- 结构分析：明确序号含义、总条数、topic 长度；新增 JSON 转义提醒
- 逐段翻译：新增**JSON 转义提醒**（英文原文里的双引号会写坏 JSON，导致整段作废重来）、
  术语一致性要求、承接上句的补全规则

### 排版精校（Word）

| 改动 | 原因 |
|---|---|
| **新增「本节目录」表**（8 段以上） | 实测这个视频 76 段、178 页，没有目录根本翻不动 |
| **段内「长难句精讲 / 重点词汇」小节标记** | 以前和正文混成一片，看不出这段有没有要细看的 |
| **附录一加段落锚点** | 纯英文版是一整块连续文本，找不到位置 |
| **时间码全篇统一成 hh:mm:ss** | 正文用 `00:01:03`、金句用 `1:03`，两种写法很扎眼 |
| Takeaways 中文用真正缩进 | 原来用全角空格凑，换字号就对不齐 |
| 标题 topic 为空时不留尾随空白 | 会出现「段落 3　」 |
| 增加规模统计行 | 「共 76 段 · 英文约 12794 词」一眼判断文档规模 |

### 实测（真实的 1 小时 17 分视频，2758 条字幕）

```
流水线完成 515 秒
段落 76 · 失败段落 0 · takeaways 5 · quotes 6 · 词汇 262
逐条中文 2758/2758（100% 覆盖）
Word 178 页 / 82,821 词 · ASS 5507 条 · SRT 204 KB
时间码格式: hh:mm:ss 82 个 / m:ss 0 个 ✅ 统一
「长难句精讲」76 处 · 「重点词汇」76 处
```

用 Word 打开逐页看过：目录表、段落标题、双语正文、词汇行、金句版式都正常。

### 关于 PROMPT_VERSION

**没有升版本**（仍是 4）。原因：这次的提示词改动是澄清与加固，不改变翻译语义；
而不升版本的话，**已缓存的视频不需要 API Key 也能继续生成文档**。
升了就必须先配 key 才能再生成——考虑到你的 key 目前是空的，这个差别很实际。

---

## v1.18.1 — 「博主」统一改成「频道」（2026-09-12）

主页标题「粘贴博主主页链接开始」→「**粘贴频道主页链接开始**」。

顺手把**所有用户可见**的「博主」都统一成「频道」——只改标题会导致
「标题说频道、下面输入框说博主」这种不一致：

| 位置 | 改后 |
|---|---|
| 主页标题 | 粘贴频道主页链接开始 |
| 输入框提示 | 粘贴频道主页链接，例如 … |
| 返回按钮提示 | 本次抓取过的频道都会留在主页上 |
| 设置 · 文件夹命名 | 频道母文件夹命名 |
| 设置 · 说明 | 以频道主页抓取的内容会先建这层母文件夹 |
| 空状态 | 还没有频道 / 这个分类下还没有频道 |
| 提示 | 请先粘贴频道主页链接 |

代码注释里的没动（不面向用户，改了两边反而容易看岔）。

---

## v1.18.0 — 下载完成后自动关机 + 网络诊断（2026-09-12）

### ① 全部任务完成后自动关机

设置里新增开关（**默认关闭**）。睡前挂一批下载、下完自动关机。

**两个安全设计**：

1. **必须真的跑过任务才允许关机**。用一个 `armed` 标志记住「见过任务在下载」，
   否则一打开软件、队列恰好是空的，就会立刻把机器关掉 —— 那是灾难。
2. **一定要能取消**。走 Windows 原生的 `shutdown /s /t 60`（任务栏会弹系统提示），
   界面上再给一条**红色闪烁的倒计时条 + 「取消关机」按钮**，一键调 `shutdown /a`。

另外：**一有新任务进来就立刻取消已排定的关机**（用户显然还没弄完）。
可选「有失败任务时也关机」，默认勾选（重试都用完了，留着也不会自己好）。

单元验证覆盖了五种情形：空队列不关机 / 有任务时上锁 / 跑完排定关机 /
新任务自动取消 / 开关关闭后不排定。

### ② 网络诊断

**起因**：装完第一次下载就失败，报 `Sign in to confirm you're not a bot`，
但界面上只有一屏英文，完全不知道为什么。

查下来真正的原因是：**出口 IP 是一台 Oracle 云服务器**（`hosting: true`）。
YouTube 对**机房 IP** 的风控极严，直连必然被拦。而用户机器上的 Clash 当时
**并没有真正代理**（走代理和直连的出口 IP 一模一样）—— 这些从报错里一个字都看不出来。

所以加了一个「网络诊断」按钮，一次说清：

- 出口 IP + 归属 + **是不是机房 IP**
- 本机在监听哪些代理端口（Clash / v2ray 等常见端口）
- 软件里当前配的代理
- yt-dlp 实测能不能读到视频
- 根据以上给出**针对性的中文建议**

### 顺带

诊断过程中确认：**Edge/Chrome 的 cookie 依旧读不了**（App-Bound Encryption，
yt-dlp 已知限制），所以 cookies.txt 仍是可靠路径。

---

## v1.17.1 — 安装程序做得像正经商业软件（2026-09-12）

原来的安装向导是 electron-builder 的默认样子：**全英文、没有品牌元素、没有许可协议**。

### 加了什么

| 项 | 说明 |
|---|---|
| **中文界面** | 标题「YouTube Downloader 安装」，按钮「我同意(I)」「取消(C)」 |
| **品牌侧边栏** | 164×314，logo + 产品名 + 四条功能 + 版本号 |
| **品牌页眉** | 150×57，出现在内页右上角 |
| **许可协议页** | 完整的软件许可与使用条款，需点击「我同意」才能继续 |
| **安装程序图标** | 用应用图标（原来是 electron 默认图标） |
| **卸载项补全** | 控制面板里「安装位置」不再是空的 |

### 踩到的三个坑

**1. 许可协议乱码**

NSIS 按 ANSI 读许可文件。用 `write` 工具写出来的 `license.txt` 是**无 BOM 的 UTF-8**，
中文全变成 `涓嬭浇鍣` 这种乱码。改成 **UTF-8 带 BOM** 才正常。

**2. 界面还是英文，尽管配了 `language: "2052"`**

查 electron-builder 源码才发现：**`language` 只用于写版本资源的 `/LANG=`，不影响安装界面**。
安装界面语言是 NSIS 在运行时按【系统 UI 语言】自动选的 ——
本机 OS 语言是 2052（中文），但**用户 UI 语言是 en-US**，所以选了英文。
真正的开关是 `installerLanguages: ["zh_CN"]`（只打包中文，强制使用）。

**3. 图片尺寸算错会溢出**

- 侧边栏内容宽只有 132px，副标题多一个字就孤字换行
- 页眉 150×57 极小：46px logo + 12px 字会**溢出换行**，缩到 38px / 10px 才放得下

素材由 `tools/make-installer-art.js` 生成（Electron 渲染 → ffmpeg 转 24 位 BMP，
NSIS 不认带 alpha 的 32 位 BMP）。

### 实测

静默安装 → 装出 440.6 MB / 25 个文件 → 程序正常运行 → 卸载退出码 0、
目录与快捷方式与注册表项全部清理干净、**用户设置与队列保留**（卸载不删数据）。

---

## v1.17.0 — 识别大频道从 2 分 45 秒降到 22 秒（2026-09-12）

### 问题：上限根本没传给 yt-dlp

`maxItemsPerChannel` 这个设置**一直存在，但只在拿到全部数据之后做过滤**：

```js
// channel.js —— maxItems 只在 pushItem 时过滤
if (maxItems > 0 && byId.size >= maxItems && !byId.has(item.id)) return;

// 而 dumpFlat 的 --playlist-end 需要 opts.playlistEnd，调用方从没传过
if (opts.playlistEnd && opts.playlistEnd > 0) args.push('--playlist-end', ...);
```

**结果：yt-dlp 把整个标签页翻完，我们再丢掉多余的 —— 网络开销一分没省。**
而且这个设置的默认值还是 0（不限），界面上也没有入口。

实测抓 **@marvel**（Marvel Entertainment）：

```
内容数: 10,616 个
耗时:   3 分 13 秒
其中 videos 标签页一个就花了 136.6 秒
```

界面一次只显示 80 条 —— 那一万条对用户毫无用处。

### 改法

1. **把上限真正传给 yt-dlp**：`maxItemsPerChannel` → `--playlist-end`
2. **容器展开也设上限**（`maxContainersPerTab`，默认 40）：
   podcasts 标签页返回的是「播放列表容器」，**每个容器是一次独立的 yt-dlp 调用**，
   Huberman 那种有 428 个 → 几百次请求，比标签页本身慢得多
3. **攒够上限就停止展开剩余容器**（展开出来也会被丢掉，纯属浪费请求）
4. 界面上加了入口，并在被截断时显示「已达识别上限」

### 实测（同一个 @marvel）

| | 耗时 | 内容数 |
|---|---|---|
| 不限（旧行为） | **165.5 秒** | 10,616 |
| 上限 300（新默认） | **22.2 秒** | 300 |

**提速 7.5 倍。** 各标签页耗时：videos 5.6s / shorts 5.9s / live 4.1s / podcasts 2.5s / playlists 4s。

### 老设置会自动迁移

你现有的 `settings.json` 里存着 `maxItemsPerChannel: 0`，会盖掉新默认值。
所以加了一次性迁移：**旧的 0 一律升到 300**，并打上 `maxItemsMigrated` 标记 ——
之后你如果想改成「不限」，显式设回 0 就不会再被覆盖。

---

## v1.16.0 — 新增 ZIP 免安装版；便携版闪退的原因查明（2026-09-12）

### 问题：便携版双击后闪退

**不是代码问题，是磁盘空间不够。** 便携版（Portable）的运行方式是把整个程序
自解压到 `%TEMP%` 再启动 —— 而 `%TEMP%` 在 C 盘。实测采样 C 盘剩余空间：

```
启动前: 1673 MB
过程中: 1523 → 1294 → 1036 → 671 → 549 MB   ← 一路下降
之后:   549 → 1674 MB                        ← 空间耗尽，自动回滚清理并退出
```

解包消耗了 **974 MB 仍未完成**，C 盘只剩 1.6 GB，于是解到一半失败、
整个目录被回滚删除、进程静默退出 —— 表现就是"双击后闪退"，**没有任何报错**。

对照实验：同一个 exe，只把 `TEMP` 指向 F 盘（412 GB 空闲），**立刻正常启动**。

### 解决：加一个 ZIP 免安装版

瘦身解决不了根本问题（砍掉 100 MB 也还是要近 1 GB 临时空间）。所以新增
**`YouTubeDownloader-Green-<版本>.zip`** —— 解压到任意盘直接运行，
**完全不需要 C 盘临时空间**：

| 产物 | 机制 | 需要 C 盘空间 |
|---|---|---|
| Setup（安装版） | 装到你选的目录 | 安装时少量 |
| Portable（便携版） | 自解压到 `%TEMP%` | **约 1 GB** |
| **Green（ZIP 免安装）** | **解压即用，无自解压** | **不需要** |

顺带裁掉用不到的语言包（`electronLanguages` 只留 en-US / zh-CN），
解包体积减少约 46 MB。

### 结论

C 盘快满时**用 ZIP 免安装版或安装版**，不要用便携版。
（或者把系统的 `TEMP` 环境变量指到别的盘，便携版也能正常跑。）

---

## v1.15.3 — 频道一天内抓过就不再重抓（2026-09-12）

v1.15.0 的 stale-while-revalidate 是「超过 30 分钟就后台再刷一遍」。
现在改成**一天**：

```js
const CHANNEL_CACHE_TTL_MS = 24 * 60 * 60 * 1000;   // 原来是 30 * 60 * 1000
```

| 缓存年龄 | 行为 |
|---|---|
| **< 24 小时** | **纯读缓存，连后台请求都不发** |
| ≥ 24 小时 | 秒开旧列表 + 后台刷新一次 |

理由：频道的视频列表不会几分钟就变，而重新枚举一个频道要十几秒、几十个请求，
还可能撞上 YouTube 风控。一天一次足够。

需要立刻拿最新列表时，点标题栏的「**重新抓取**」——那条路走 `force=true`，始终绕过缓存。

顺带把所有注释里的「30 分钟」也改掉了（代码和文档对不上，下一个人会被误导）。

---

## v1.15.2 — 主页上下各占一半（2026-09-12）

原来上半的粘贴区只占约 1/3 高度（实测 3%~32%），书签库占了下面 2/3。
改成**两块平分高度**：

```css
.home-hero    { flex: 1 1 50%; justify-content: center; }  /* 上半区里垂直居中 */
.home-library { flex: 1 1 50%; }
```

书签栏因此落到界面中线附近，下半正好是一半。

---

## v1.15.1 — 不再显示「该频道没有 XX 标签页」（2026-09-12）

列表上方原来会挂一排黄条：

```
ℹ Live 直播：该频道没有 Live 标签页
ℹ Podcasts：该频道没有 Podcasts 标签页
ℹ 播放列表：该频道没有 播放列表 标签页
```

**这不是警告，是常态** —— 大部分频道本来就没有 Podcasts / Live。纯噪音，去掉。

改了两处，**缺一不可**：

1. `channel.js` 不再往 `warnings` 里塞这条（只记进 `tabStatus`，分类标签仍靠它显示"缺这个标签"）
2. `app.js` 的渲染层**也过滤一遍**

第 2 条是关键：频道缓存（30 分钟磁盘缓存 + 会话缓存）里**已经存了旧数据带的这些警告**，
那些频道走的是「直接用缓存」路径、根本不会重新生成 —— 只改源头，缓存过的频道照样显示。

过滤按 `level === 'info'` 或消息含「没有…标签页」，实测：
- MrBeast 的 3 条 info 全隐藏 ✅
- 真实的错误（超时 / 429 / 风控）3 条全保留 ✅

### 顺带：标签栏里那几个 0 的标签也去掉了

同一个信息换了地方显示——「Live 直播 0 / Podcasts 0 / 播放列表 0」原来会灰着留在标签栏里。
空的分类现在直接不显示标签。（`.tab.missing` 样式保留：万一某个标签"缺失"但视频被交叉归类进来，仍会用到。）

---

## v1.15.0 — 抓过的频道秒开：先用旧列表，再后台悄悄更新（2026-09-12）

### 问题：**30 分钟这个门槛用错了地方**

```js
if (cached.data && age < 30 * 60 * 1000) {   // 超过 30 分钟就当作没有缓存
```

超时就直接重新联网抓。于是**每次点回主页的频道都要干等十几秒**，
而绝大多数时候列表根本没变 —— 30 分钟的缓存对新旧判断是合理的，
但把它当成"能不能用"的门槛就错了：旧的只是**可能**过期，补齐就行，不该让用户等。

### 改法：stale-while-revalidate

```
点频道 → 有缓存？→ 立刻渲染旧列表（0 秒，不显示贾维斯球）
                 ↘ 同时看年龄：超过 30 分钟就后台静默抓一遍
                                 抓完推 channel:refreshed → 界面自动替换
```

- **30 分钟内**：直接用，不刷新（不白费流量）
- **超过 30 分钟**：先秒开旧列表，标题栏显示「（2 小时前的列表 · 正在后台更新…）」，后台抓完变成「（刚刚更新）」并弹一条提示
- **没有缓存**：才走原来的路（显示贾维斯球）

### 几个必须处理的细节

1. **贾维斯球不能一闪而过**：命中缓存时 IPC 几十毫秒就回来了，
   立刻显示球会让界面抽搐。改成延迟 280ms 再显示 —— 快路径根本看不到球。
2. **后台刷新要让开**：用户主动抓取时先取消后台任务，避免两个 yt-dlp 同时打 YouTube。
3. **刷新结果要认频道**：后台任务可能在用户切走之后才回来，
   用频道 URL 比对，不是当前频道就丢弃，否则会把用户正在看的列表冲掉。
4. **保住操作现场**：更新时保留滚动位置、勾选、当前分类页、搜索词，
   否则用户翻到一半列表自己跳回顶部、勾选也没了，比不更新还烦。

---

## v1.14.1 — 贾维斯球改成 HUD 仪表盘风格（2026-09-12）

原来的球是「三个环 + 一个核心」，太简单。改成参考 HUD 仪表盘的层次，
由外到内共九层：

**外发光 → 点阵环 → 主刻度环（每 30° 一根）→ 次刻度环（每 6° 一根）
→ 外细线环 → 旋转弧刃 ×2 → 内环 → 发光核心**

刻度与点阵都用 `repeating-conic-gradient` 画，再用 `radial-gradient` 遮罩裁成"环带"，
**不用任何图片或 SVG**。两层弧刃反向旋转做出涡轮感。

### 踩到的坑：图层全部塌在一起

第一版渲染出来只剩一个白球，刻度环和弧刃全不见。原因是 CSS 特异性：

```css
.jv-orb > span { inset: 0 }     /* 特异性 (0,1,1) */
.jv-core { inset: 38% }         /* 特异性 (0,1,0) —— 被上面覆盖！ */
```

子选择器 `.jv-orb > span` 比单独的类名更"具体"，所以各层的定位全被重置成 `inset: 0`，
九层图元叠在同一位置、后面的盖住前面的。
修法：基础规则里**不写 `inset`**，只留 `position`/`border-radius`，让各层自己声明。

### 第二个问题：太糊，不像 HUD

初版外发光（`drop-shadow` 18px / 核心 `box-shadow` 56px）过重，把锐利的刻度线和弧刃糊成一片光晕。
收窄到 9px / 30px，并把弧刃的明暗过渡改短，才做出仪表盘的清晰感。

**验证方式**：截图后用 ffmpeg 裁出球的区域放大 3 倍看细节 —— 全屏截图里球太小，看不清层次对没对。

---

## v1.14.0 — 主页改版：书签式分类库 + 贾维斯球（2026-09-12）

### ① 主页分成上下两半

原来所有东西挤在中间垂直居中。现在：

- **上半**：品牌图 + 标题 + 说明 + 示例链接，靠上排布
- **下半**：书签式分页的频道库

### ② 按内容倾向自动归类

书签栏按博主的内容倾向分组，点标签切换，每个标签带数量。

**判定依据是视频标题的内容倾向，不是频道名** —— 「Mel Robbins」这个名字本身看不出
「个人成长」，但它的视频标题里满是 motivate / habit / mindset，一眼就能归对。

六个分类 + 兜底：**🤖 科技 & AI** / **🌱 个人成长** / **🧬 健康 & 科学** /
**📈 商业 & 财经** / **📚 人文 & 历史** / **🍿 娱乐 & 生活** / **📁 其他**

**为什么不用大模型分类**：粗粒度归类关键词打分就够，而且**不花钱、不联网、瞬时完成**；
识别频道时手上正好有几百条标题，是最准的时机。

实测四个真实频道全部归对：

| 频道 | 归类 | 得分 |
|---|---|---|
| OpenAI | 🤖 科技 & AI | 713（第二名 63） |
| Lex Fridman | 📚 人文 & 历史 | 813（科技 471） |
| Mel Robbins | 🌱 个人成长 | 468（健康 247） |
| Huberman Lab | 🧬 健康 & 科学 | 488（成长 172） |

分类在**识别频道时**算好并写进 `channels.json`；老记录启动时按频道缓存里的标题**自动回填**
（不用重新联网识别）。同名的关键词用词边界匹配，避免 `ai` 命中 `said`、`again`。

### ③ 抓取时的「贾维斯球」

点任何博主开始抓取，内容区浮出一个旋转的能量球，**转到内容读完为止**，下方实时显示进行到哪一步。

三个光环**绕不同轴旋转**（Z / X 72° / Y 72°），形成陀螺仪的立体感，而不是三个平面同心圆。
做法是用 CSS 的 `rotate` 独立属性设静态倾角、`transform` 交给动画转 —— 两者会叠加，
这样才不会被动画的 `transform` 覆盖掉倾角。

---

## v1.13.0 — 金句卡片：把金句渲染成图片（2026-09-12）

学习文档里的「金句总结」现在可以一键变成图片，存手机 / 分享都方便。
队列卡片上多了个「**金句图**」按钮（只有生成过学习文档的任务才有——金句来自文档的分析结果）。

产出放在视频旁边：`<视频名>.金句卡片/金句-01.png`，规格 **1080×1350**（4:5 竖版）。
版式：YouTube logo + 频道名 → 引号 → 英文金句（Times New Roman）→ 中文（琥珀色）→
分隔线 → 视频标题 + 时间码。**不产生任何 API 费用**，金句直接从学习缓存里取。

英文长度差异很大（有的两行、有的七行），所以字号会**自动收缩**到恰好装下，绝不裁切或溢出。

### 实现方式与踩到的三个坑

用 **Electron 渲染 HTML + `capturePage()` 截图**，而不是 ffmpeg 的 `drawtext`：
drawtext 画不了中英混排、自动折行、渐变背景，CJK 换行尤其难控。
用浏览器渲染则可以完全用 CSS，排版质量和软件界面一致。

过程中踩了三个坑，都靠最小用例定位出来了：

| 现象 | 真实原因 |
|---|---|
| `ERR_FAILED` 加载失败 | 我以为是 `sandbox: true`（沙箱渲染进程没有文件权限），改掉后**仍然失败** |
| 截图只有 1080×1032，底部被裁 | **窗口尺寸在「创建时」会被钳制到屏幕工作区**（本机 1920×1032，连 1080 高都开不出来） |
| 第二张卡片起全部失败 | 每渲染一张就建/销毁一个窗口，销毁唯一窗口触发了 Electron 的 `window-all-closed` 默认退出 |

**解法**：先创建一个小窗口，**创建之后**再 `setBounds({ width: 1080, height: 1350 })` —— 实测能突破钳制，
拿到完整的 1080×1350。（真实 App 里主窗口一直在，所以第三条不会发生。）

还有一个反直觉的结论：**隐藏窗口截图是可行的**，`offscreen: true` 反而不必要（实测两者结果一致）。

---

## v1.12.1 — 空状态的大图标换成 YouTube logo（2026-09-12）

主页空状态原来是个 📺 emoji，现在换成官方 YouTube logo 图片（`src/renderer/youtube-logo.png`），
和顶栏品牌标、窗口图标用的是同一张素材，三处统一。

logo 是 1.44:1 的横长方形，所以 CSS 给宽度、高度自适应，不做拉伸。

---

## v1.12.0 — 失败重试改成分级退避（2026-09-12）

### 问题：重试是「立即」的，而风控偏偏需要时间

```js
item.status = 'queued';   // 立刻放回队列
this.pump();              // 马上又开始下载，没有任何等待
```

`autoRetry = 3` 时，三次重试会在**两三秒内全部烧光**，任务直接判死。
可风控和 429 恰恰是**等一会儿就自己好**的错误 —— 实测这个风控几分钟就过去了。
等于白白浪费了全部重试机会。

### 改法：先分清错误类型，再决定等多久

| 类型 | 触发条件 | 退避节奏 |
|---|---|---|
| **限流 / 风控** | `Sign in to confirm`、`HTTP 429` | 20 秒 → 1 分 → 3 分 → 5 分 |
| **网络抖动** | 超时、连接重置、5xx | 3 秒 → 10 秒 → 30 秒 |
| **永久性错误** | 视频不存在 / 私享 / 会员专属 | **不重试**，直接失败 |

- 退避中的任务不再被 `pump()` 启动，由一个定时器盯住最早到点的那个唤醒（不轮询）
- 队列卡片显示**实时倒计时**：「⏸ YouTube 限流，18 秒后自动重试」
- 多一个「**立即重试**」按钮，不想等就直接点
- 永久性错误不再浪费重试次数

### 实测（真实风控环境下）

```
[  0.0s] downloading 开始下载
[queue] throttle 错误，20s 后重试
[  2.8s] queued      失败，20 秒后自动重试（第 1 次）
[ 22.8s] downloading 开始下载                       ← 20 秒后自动重试
[queue] throttle 错误，60s 后重试
[ 25.0s] queued      失败，1 分钟后自动重试（第 2 次）  ← 退避翻倍
```

改之前：**6 秒内 3 次重试全烧完，任务失败**。

另外实测确认：**切换 player_client（tv / ios / android / tv_embedded 等 8 种）绕不过这个风控** ——
它是按 IP 判定的，不是按客户端。所以走"自动等待"而不是"换花招"。

---

## v1.11.0 — Cookies 一键配置 + 修掉「配了 Cookie 却没传全」（2026-09-12）

### ① 修掉一个让 Cookie 形同虚设的 bug

之前 **Cookies 和代理只加在「下载」那一步**，而频道识别（`dumpFlat`）、
视频探测（`probeVideo`）、作品详情（`videoDetails`）、**抓字幕**（独立的一次 yt-dlp 调用）
全都没带 —— 于是出现最讽刺的一幕：**YouTube 报错让你配 Cookie，你配了却依然被拦。**

现在新增 `src/main/ytdlp-auth.js` 统一生成身份/网络参数，**所有 yt-dlp 调用共用同一套配置**：

| 调用 | 之前 | 现在 |
|---|---|---|
| 下载 | ✅ 有 | ✅ |
| 抓字幕 | ❌ 没有 | ✅ |
| 频道识别 | ❌ 没有 | ✅ |
| 播放列表展开 | ❌ 没有 | ✅ |
| 视频探测 / 作品详情 | ❌ 没有 | ✅ |

### ② 「从浏览器读取 Cookies」+ 实测按钮

设置里新增浏览器下拉框，取值直接给 yt-dlp 的 `--cookies-from-browser`，
本机装了哪些浏览器会自动标注（没装的标灰）。旁边的**「测试是否生效」按钮**用固定视频
实打一次请求 —— **只配了不算数，YouTube 认不认要试了才知道**。

### ⚠️ 实测结论：本机 Edge 读不了（重要）

| 浏览器 | 结果 |
|---|---|
| **Edge** | ❌ Cookie 库存在，但报 `Failed to decrypt with DPAPI` |
| Firefox | ❌ 装了但从没用过，Profiles 目录是空的 |

原因是 **Chrome / Edge 127+ 启用了「应用绑定加密」（App-Bound Encryption）**，
第三方工具（含 yt-dlp）无法解密其 Cookie 库 —— 这是 yt-dlp 的已知限制，不是本程序的 bug。

**所以在这台机器上，可靠路径是导出 `cookies.txt`**（浏览器扩展导出，程序里选文件即可）。
各种失败原因都翻译成了能照着做的中文提示（加密读不了 / 库不存在 / 被占用 / 仍被风控）。

---

## v1.10.2 — 图标改用官方素材 + 去掉顶栏内核状态小字（2026-09-12）

### ① 图标改用官方 logo 素材

v1.10.1 里我用代码**手搓**了一版 YouTube logo（对齐官方 SVG 的几何参数），
这次直接用官方素材 `build/youtube-logo.png`（640×443，带透明通道）——还原度更高。

`tools/make-icon.js` 改为：素材按比例缩放到目标宽度后**居中放进透明正方形画布**
（官方素材是 1.445:1 的横长方形，直接铺满正方形会被拉伸变形），
缩放与补边交给内置 ffmpeg（lanczos 重采样），脚本只负责把各尺寸 PNG 打包成 ICO。

### ② 去掉顶栏「内核就绪」那行小字

品牌区只留「YouTube 下载器」标题。内核状态改到设置里查看，并保留一条兜底：
**内核真的不可用时弹一次提示**——否则用户只会觉得"点了没反应"，无从判断原因。

### ③ 顶栏品牌标也换成同一个 logo

原来顶栏左侧是 CSS 画的红方块 + `▶` 字符，跟新的应用图标不一致。
改成直接用 `src/renderer/youtube-logo.png` 图片（`img-src 'self'` 已在 CSP 里允许）。

---

## v1.10.1 — 换 YouTube 原版图标 + 去掉预览文案 + 去掉绿字（2026-09-12）

### ① 图标换成 YouTube 原版

原先的图标是「红橙渐变的圆角方块 + 白三角」——通用播放按钮，不是 YouTube 的 logo。
换成按官方几何重做的：**纯红圆角矩形（宽高比 1.43:1）+ 白色播放三角**。

新增 `tools/make-icon.js` 可重新生成，参数对齐官方 SVG 的几何
（矩形 546×384、圆角 0.18 高、三角高 0.42 高、三角宽高比 0.88、三角右偏 0.10 个三角宽）。
纯手写像素渲染 + 4 倍超采样抗锯齿（小尺寸用 8 倍），自己编码 PNG 再打包成 ICO——
**不引入任何图像库**（项目里没有 sharp / canvas，也不该为一张图标引入）。
产出 7 个尺寸（16/24/32/48/64/128/256）。

### ② 预览里的文案去掉

「作品详情」弹窗不再显示文案——列表用的 flat 数据本来就没有文案，
为此专门联网拉一次属于多此一举。现在只保留：大图封面、标题、频道、发布时间、
播放量、点赞、时长，以及「下载这个 / 打开所在文件夹 / 在 YouTube 打开」。

边车文件里也不再存文案（原来截断存 4000 字符，现在没人看，白占空间）。
`--write-info-json` 仍然保留——发布时间、播放量、点赞还是要从它里面取。

### ③ 「内核就绪」不再显示绿字

原来用主题色 `--ok`（绿），太跳。改成和旁边一致的灰色。

---

## v1.10.0 — 按 ID 去重 + 任务持久化加固 + 作品详情预览（2026-09-12）

### ① 按作品 ID 检测本地文件，已下载的自动标记并跳过

难点在于**文件名模板不含视频 ID**（是「标题 [日期].mp4」），光看文件名根本判断不了某个作品下没下过。

做法：下载完成后在视频旁边写一个 `<文件名>.video.json`（约 0.6 KB 的**边车文件**），
里面记着视频 ID、标题、发布时间、播放量、文案等。于是：

- 「是否已下载」**完全以磁盘为准** —— 扫描下载目录即可还原全部记录
- 队列被「清除已完成」清空、换台机器、重装软件，都不影响识别
- 启动时自动扫描（5 秒缓存避免反复扫盘），列表上打「已下载」标记
- 视频文件被手动删掉 → 该记录自动作废，不会误判成"已下载过"而永久跳过
- 历史下载会**自动补写边车**（实测为现有 2 个下载补写成功）

### ② 任务记录持久化

先实测确认：各种状态（done / error / queued / paused / skipped / canceled）
在重启后**全部保留**，失败原因和文件路径也在——这块本来就正常。

本次**加固**：原先所有改动统一走 400ms 防抖，强杀进程或断电会丢掉最后几秒的操作。
现在**重要事件（入队、状态流转、完成、失败）立即同步落盘**，
只有进度更新仍走防抖——那种一秒几十次的写入才真正需要节流。

### ③ 点封面看作品详情

点列表里的封面打开预览：大图封面、标题、频道、发布时间、播放量、点赞、时长，
以及**完整文案**。数据分三步来，先出再补：

1. 列表里已有的（标题/封面/播放量/时长）→ 立即渲染，不等网络
2. 下载过的 → 读本地边车，发布时间与文案秒出
3. 联网拉最新详情（含文案）

下载时顺带加 `--write-info-json`（**零额外网络请求**，元数据本来就抓过了），
读出来写进紧凑边车后再把那份大的删掉 —— 这样**下载过的作品离线也能看文案**。
点开会自动作废在途请求（连点几个封面时，早先的请求回来得晚，不会覆盖当前正在看的）。

### 已知限制

**YouTube 风控会挡住「点开时联网拉详情」。**
实测在频繁请求后会出现 `Sign in to confirm you're not a bot`。
此时下载过的作品仍能从本地边车显示完整信息；未下载的只能显示列表里已有的字段。

---

## v1.9.0 — 字幕字体可选 + 中英行距可调（2026-09-12）

### ① 中英行距：把英文往下拉

原先中英两行在同一个事件里用 `\N` 换行，**行距完全由 libass 按字体度量决定**，
实测恒等于英文的下伸部空间（1080p / 字号 50 下 8px），而 ASS 根本没有行距标签可调。

改成**中英拆成两个事件、各自用 `\pos` 精确定位**。这是靠逐像素实测定下来的方案：

- 行推进 = 字号 × 1.000（两种字体都一样）→「块高 = 行数 × 字号」是精确的
- `\pos` 定位的事件**不参与** libass 的防重叠计算，位置完全可控
- 英文底边 = 中文底边 − 中文行数×中文字号 − 额外行距，**改 1px 就精确移动 1px**

新增设置「中英行距」（单位＝英文字号的倍数，默认 **-0.2**）：
0 是 libass 自然间距，越负越紧凑。实测 -0.2 在 1080p 与竖屏 1920 下都明显收紧，
带下伸部的字母（g/y/p）仍不会碰到中文。

> 顺带记一笔：`\fs` 配 `\fscx/\fscy` 把字形缩放回来是**无效**的，
> libass 仍按缩放后的高度算行距（实测间隙不变，白折腾）。

### ② 字幕字体可自选

设置里新增**英文字体 / 中文字体**两个输入框，可从系统已安装字体下拉选择，也可直接输入字体名。

- 默认：英文 `Times New Roman`，中文 `微软雅黑`
- 字体列表用 .NET 的 `InstalledFontCollection` 读取（实测 164 个族），
  **不用注册表**——注册表里的值名是「Microsoft YaHei & Microsoft YaHei UI (TrueType)」这种合并串，
  拆起来容易出错，而且中文系统下会漏掉微软雅黑、宋体
- .NET 报的是英文族名（`Microsoft YaHei`），但 libass 也认中文名（`微软雅黑`），
  所以额外补了几个常见中文字体别名，两种写法都能选、都能渲染

### ③ 顺带修掉字幕里的一处垃圾字符

YouTube 会把消音占位 `[ __ ]` 转义成 `[\h__\h]`（`\h` 是 ASS 的硬空格转义）。
原先清洗时直接删反斜杠，结果字幕里出现 `[h__h]` 这种垃圾——
**实测一条 1 小时的视频里有 204 处**。现在在清洗和 ASS 转义两处都先把 `\h` 还原成空格。

---

## v1.8.2 — 金句时间码改到句末同行（2026-09-11）

时间码原先单独起一行，而 `NoteQ` / `NoteA` 有 240 的缩进、`MetaLine` 没有，
所以时间码贴在左边距上，**看起来更像下一句的标签**，分不清属于哪句。

改为**接在英文句末**，同一段落内用较小字号和灰色显示：

```
◆ Do you understand who you consume? What you consume is who you become.　[0:12]
　 你明白你在消费谁吗？你消费什么，就会成为什么。
◆ People are obnoxiously more capable of changing than they realize.　[4:37]
　 人们改变的能力远超自己的想象，简直令人讨厌。
```

---

## v1.8.1 — 文档排版微调（2026-09-11）

- **Takeaways 的条目改用黑色圆点 `●`**，不再用数字序号——这些要点是并列关系，没有先后次序
- **去掉「金句总结」标题下那句说明**（「共 N 句，由大模型从全片中挑出；方括号内是原片时间码」）

---

## v1.8.0 — 学习文档新增 Takeaways 与 金句总结（2026-09-11）

### ① 文首新增 Takeaways（3~5 条全文要点）

位置就在「频道 / 时长 / 上传 / 链接」下面、正文之前。
每条给**英文一句话 + 对应中文**，与全文的中英对照风格一致。

### ② 文末新增 金句总结（3~6 句）

放在整份文档的**最后**（附录之后），独立起一页。每句包含英文原句、中文翻译、
以及**原片时间码**（方括号内，方便回视频里找）。

### 关键实现：不增加 API 调用

结构分析那一步本来就通读全文，所以让它**顺便**输出 takeaways 和 quotes —— **零额外请求**。
结构分析的返回格式从数组改为对象：

```json
{"plan":[...], "takeaways":[{"en","zh"}], "quotes":[{"cue","en","zh"}]}
```

`cue` 是字幕条序号，用来换算成时间码。

### 老文档不必重新翻译

提示词升级会让旧缓存失效。但逐段翻译要几十次调用，而缺的只是结构分析**一次调用**的产物。
所以加了条轻量路径：**复用已有的全文翻译，只补跑一次结构分析**，把 Takeaways / 金句补上再重排版。

缓存新增 `summaryDone` 标记：没有这个标记的（旧版本写的、或补总结失败留下的）
一律当作不完整，走轻量路径补全，而不是拿来就用导致文档缺内容。

### 实测

用已有的《Introducing the Agents API》文档验证：产出 5 条 Takeaways + 5 句金句
（含 `[0:00]`~`[2:03]` 时间码），Word 里结构标记的位置：

| 段号 | 内容 |
|---|---|
| 5 | Takeaways |
| 123 | 附录一　纯英文版 |
| 133 | 附录二　词汇总表 |
| 340 | 金句总结（文档最后） |

整份文档 357 段。

---

## v1.7.1 — 去掉识别流程的冗余弹窗（2026-09-11）

识别成功时会弹出「正在抓取「X」的最新内容…」「识别完成，共 N 个内容」这类提示，
但上方状态栏本来就在显示进度、列表也直接出来了，**弹窗只是噪音**。已去掉：

- ~~正在抓取「X」的最新内容…~~（点博主头像时）
- ~~切换到「X」（本次已抓取，直接复用）~~
- ~~识别完成，共 N 个内容~~

**保留**的都是有实际信息量的：

| 提示 | 为什么保留 |
|---|---|
| 识别失败：… | 失败必须让用户知道 |
| 已取消识别 | 确认操作生效 |
| 没有识别到任何内容… | 说明为什么列表是空的 |
| 有 N 个分类抓取失败 | 部分失败，结果不完整 |
| 正在识别中，请稍候… | 点击时任务在跑，解释为什么没反应 |

---

## v1.7.0 — 返回主页 + 本次抓取永不重复抓取（2026-09-11）

### ① 频道页右上角新增「← 返回」

一键回到主页。本次抓取过的博主会**留在主页上**，可以直接点回去。

### ② 会话缓存：本次运行内抓过一次，就绝不再抓

原先只有磁盘缓存，**TTL 是 30 分钟**——超过就会重新抓一遍。
现在在主进程里加了一层**内存会话缓存（无过期时间）**：

- 命中会话缓存时**不发任何请求**，直接复用，界面提示「本次已抓取，直接复用」
- 会话缓存优先于磁盘缓存，磁盘缓存仍保留（用于跨次启动）
- 点「重新抓取」仍然绕过两层缓存，强制重新抓
- 关闭程序后自动清空（内存里的东西，不落盘）

### ③ 主页新增「本次已抓取」快捷入口

主页上列出本次运行内抓过的**频道 / 播放列表 / 单个视频**（头像 + 名称 + 内容数），
**点一下直接切回**。配合会话缓存，多个博主之间来回切换是**秒开**的，不会重新抓取。

与已有的「最近下载的博主」并列显示，两者用途不同：

| 区块 | 内容 | 点击行为 |
|---|---|---|
| **本次已抓取** | 本次运行内识别过的（内存，关掉即清） | 立即切回，**不重新抓取** |
| **最近下载的博主** | 历史上真正下载过的（落盘，按下载数排序） | 抓取其最新内容 |

---

## v1.6.2 — 补齐「生成文档」入口 + 启动自愈（2026-09-11）

v1.6.1 修好了字幕匹配，但**修复够不着入口**：那条任务的卡片上只有「打开」和「✕」，
连「重做」按钮都不显示。

### 根因：按钮的显示条件本身就依赖出错的那个字段

v1.3.1 精简按钮时写成了：

```js
if (q.studyDocPath)       → 「重做」
else if (q.subCount > 0)  → 「生成文档」
```

`subCount` 正是被文件名截断 bug 弄丢的那个值。它一为空，**两个条件都不满足，
按钮整个消失**，用户连"重新试一次"的机会都没有——错误状态把自救入口也一起锁死了。

### 修复

1. **按钮条件改为看「下载完成 + 文件还在」**，不再依赖 `subCount`。
   只要视频还在，就给一个「生成文档」入口（生成前会重新扫盘找字幕）。
2. **启动时自愈**：队列加载时，对「已完成但没记到字幕」的任务重新扫一遍磁盘，
   把漏记的字幕补回去。这样**打开 App 就能看到字幕数恢复、按钮出现，不需要重新下载**。

### 验证（用真实队列实测）

```
[queue] restored 2 item(s)
[queue] 已补回 1 个任务遗漏的字幕记录
  This One Episode Will Motivate You to Chan
    subCount = 1  -> ... Gary Vee [2026-09-07].en.srt   ← 从 0 恢复到 1
  Introducing the Agents API
    subCount = 1  -> Introducing the Agents API [2026-09-10].en.srt
```

---

## v1.6.1 — 修复「字幕下好了却判为没有字幕」（2026-09-11）

### 症状

下载完成、字幕也下好了，但任务显示「已完成（视频 + 音频）」，
学习文档报 **「没有英文字幕，无法生成学习文档」**。

### 根因：文件名被截断导致字幕匹配失败

目录里的实际情况：

```
名称长度 86  |  ... Gary Vee [2026-09-.mp4        ← 视频名被截断
名称长度 86  |  ... Gary Vee [2026-09-.mp3        ← 音频名被截断
名称长度 92  |  ... Gary Vee [2026-09-07].en.srt  ← 字幕名【没】被截断
```

**字幕其实就在同一个文件夹里**，但两处对不上：

1. 视频下载带了 `--trim-filenames 180`（路径超长时截断文件名），
   而**字幕抓取那一步没有带**，所以字幕保留了完整文件名
2. `collectSubtitleFiles()` 用「视频名 + `.`」做前缀匹配找字幕——
   截断后的视频名匹配不上完整长度的字幕名，于是 `subPaths` 为空

在「博主母文件夹 / 视频文件夹 / 文件」三层结构下路径更长，更容易触发截断，
所以这个问题在 v1.6.0 之后变明显了。

### 修复

1. **`collectSubtitleFiles()` 兼容截断**：截断只砍尾部，所以视频名与字幕名必然**互为前缀**
   （较短者是较长者的开头）。除了原有的「视频名 + `.`」精确匹配，再加一条前缀匹配，
   阈值 24 字符以免误配同目录下名字相近的其它视频（已用两个相似标题实测无互相误配）。
2. **`--trim-filenames` 180 → 220**：三层结构下减少不必要的截断。
3. **新增 `refreshSubPaths()`**：生成学习文档前重新扫一遍磁盘上的字幕。
   这样**已经下载完成的历史任务，点一下「重做」就能直接生成学习文档，不必重新下载**。

### 验证

| 检查 | 结果 |
|---|---|
| 用真实被截断的路径调用 `collectSubtitleFiles` | ✅ 找到 1 个字幕 |
| 两个相似标题（#400 / #401）是否互相误配 | ✅ 各自只匹配到自己的字幕 |
| 已损坏的历史任务调用 `refreshSubPaths` | ✅ `subPaths` 从空变为找到 `.en.srt` |

---

## v1.6.0 — 博主母文件夹（2026-09-11）

以**博主主页**抓取的内容，现在先在下载目录下建一层**博主母文件夹**，
每个视频自己的文件夹放在它里面；母文件夹已存在则直接复用。

```
F:\YouTube下载\
  └ @lexfridman\                                    ← 博主母文件夹
      ├ World’s Largest Tennis Match [2026-08-23]\   ← 视频自己的文件夹
      │   ├ ....mp4
      │   ├ ....mp3
      │   ├ ....zh-en.ass
      │   └ ....学习文档.docx
      └ Another Video [2026-01-01]\
          └ ...
```

### 母文件夹的命名方式（设置里可选）

| 选项 | 结果 | 说明 |
|---|---|---|
| `@频道名`（默认） | `@lexfridman` | 最易读 |
| 频道 ID | `UCSHZKyawb77ixDdsGog4iWA` | YouTube 内部 ID，最稳定（改名也不变） |
| 频道显示名 | `Lex Fridman` | 可读但可能含空格/特殊字符 |

字段缺失时会逐级兜底：所选项 → `@handle` → 显示名 → 频道 ID。
`@handle` 还能直接从主页地址推断，因此即使识别结果里没带也能用。

### 适用范围

- **以博主主页抓取后下载的**（含在主页里展开播放列表后下载的）→ 套母文件夹
- **直接粘贴单个视频链接** → 不套（没有所属主页的概念）
- **直接粘贴播放列表链接** → 不套

### 实现要点

- `effectiveTemplate()` 改为可拼三层：`博主母文件夹/视频文件夹/文件名`
- 母文件夹名是**字面量**，插进 yt-dlp 的 `-o` 模板前会把 `%` 转义成 `%%`，
  否则频道名里的 `%` 会被当成模板占位符
- Windows 非法字符（`\ / : * ? " < > |`）统一替换为 `_`，并去掉结尾的点和空格
- 字幕抓取那一步单独跑 yt-dlp，也使用同一个模板，所以字幕/ASS/Word 一样落在母文件夹内

### 说明

已有的平铺文件**不会自动搬家**；新下载的才会按新结构存放。

---

## v1.5.1 — 修复「删了文件仍判为已下载」「换清晰度下不动」（2026-09-11）

### 根因：yt-dlp 的 `--download-archive`

它按**视频 ID** 记账，一旦记上就无条件跳过——**既不检查文件是否还在，也不区分清晰度**。
于是出现两个症状：

1. 下载过的视频，**手动把文件删了**，重新下载仍然被跳过
2. **下过任意一个清晰度**之后，再换其他清晰度也会被跳过

### 修法

**① 不再使用 `--download-archive`**，改为依赖 yt-dlp 的默认行为：
目标文件存在就跳过、不存在就下载。判断依据从"历史记录"换成"磁盘上文件是否真的在"。

**② 判断「已下载」时验证文件是否存在**。
队列快照新增 `fileExists` 字段；渲染层把它计入「已下载」，用于行上的标记和「仅显示未下载」过滤。
文件被手动删掉的视频会重新显示为"未下载"，可以正常重下。

**③ 换清晰度时先删掉旧文件**。
文件名模板不含清晰度（如 `标题 [日期].mp4`），换清晰度还是同一个文件名，
yt-dlp 会误判成"已下载"而跳过。现在会记录本次下载的**规格**（清晰度 + 编码策略），
规格变化时自动删掉旧文件（连同 `.part` 分片和旧的 mp3）再重新下载，目录里不会留下两份。

**④ 设置项语义更新**：「跳过已下载过的视频」→「**跳过磁盘上已存在的文件**」。
关闭时表示"明确要求重下"，同样会先删旧文件。

### 验证

用一个预置了 `download-archive` 记录的临时环境实测：

| 场景 | 结果 |
|---|---|
| archive 有记录、但文件不存在 | ✅ 真的重新下载（944 KB） |
| 文件被手动删除后重新下载 | ✅ 真的重新下载（944 KB） |
| 换成 720p 重新下载 | ✅ 944 KB → 1874 KB，且目录里只有 1 个文件 |

### 说明

旧的 `download-archive.txt` 已不再被读取，可以忽略或手动删除。
**断点续传不受影响**：续传靠 yt-dlp 的 `--continue` 和 `.part` 文件，与 archive 无关。

---

## v1.5.0 — 「最近下载的博主」快捷入口（2026-09-11）

首页空状态下方新增一块**博主快捷入口**：显示头像，**按该博主的下载数倒序**排列，**点一下直接开始识别**它的最新内容。

### 数据来源与计数

- 新增 `userData/channels.json` 记录每个博主的 `{ url, title, avatar, downloads, lastAt }`
- 视频**下载成功或已存在**时，给它所属的博主计数 +1（因此这个数字是"实际下过的数量"，不是"点过几次"）
- 入队时会记录该批视频所属的博主；频道改名或换头像会自动更新
- URL 做了归一化（去掉结尾斜杠、忽略大小写），`@LexFridman/` 与 `@lexfridman` 视为同一个
- 面板最多显示 12 个；**右键可从列表移除**（只移除记录，不影响已下载文件与队列）

### 历史回填

老版本下载的任务只记录了频道**名字**没有 URL，所以启动时会做一次**一次性回填**：
借频道缓存（里面存着每个频道的完整识别结果，含 title 与 url）建立「名字 → URL」映射，
把历史下载补进统计。用设置项 `channelsBackfilled` 作标记，保证只跑一次、不会重复累加。

### 已知限制

面板统计的是**实际下载数**，因此刚装好或队列为空时面板不显示——下载任意视频后就会出现。
（如果希望把"识别过但还没下载"的频道也放进来，可以再加。）

---

## v1.4.1 — 「全部取消」（2026-09-11）

下载队列头部新增 **「全部取消」** 按钮：

- **立即停止**所有正在下载的任务（连同 ffmpeg 子进程一起杀掉）
- 把队列里**所有未完成**的任务移除（等待中 / 下载中 / 已暂停 / 失败 / 已取消）
- **保留**已完成（done）与已存在（skipped）的记录——它们对应磁盘上真实的文件
- 因为是不可逆的批量操作，点击后会弹**原生确认框**，并显示将被移除的数量；
  确认框默认停在「取消」上，避免误按回车

**使用场景**：批量选中一个大播放列表（或整个频道）开始下载后，
队列里会瞬间堆起成百上千条任务；想推倒重来时用这个按钮比逐条删除快得多。

> 部分下载的分片（`.part`）会保留在磁盘上，不会删除；如需清理请手动删。
> 另外，取消后如果重新加入同一个视频，断点续传依然有效。

---

## v1.4.0 — 支持读取频道「播放列表」（2026-09-11）

频道识别从 4 个标签页扩展到 5 个：**Videos / Shorts / Live 直播 / Podcasts / 播放列表**。

### 播放列表：先列出，点开才展开

**像 YouTube 原页面一样，Playlists 标签页先把播放列表本身列出来**（缩略图 + 标题），
**不做预展开**；点击某个播放列表才去抓它的视频，展开后视频以缩进层级显示在该列表下面。

- 每个播放列表左侧有勾选框：**勾选 = 展开并把这个列表的视频全部加入选择**
- 未展开时显示「点击展开查看视频」；展开后显示「N 个视频」，勾选后显示「N 个视频 · 已选 M」
- 已展开的视频会合并进总列表，**与 Videos 标签页里的同一条视频共用同一个对象**，
  因此在哪边勾选状态都一致；下载时按视频 id 去重，不会重复下载

**为什么不做预展开**：实测 @lexfridman 有 13 个播放列表、@hubermanlab 22 个、**@OpenAI 58 个**。
预展开要逐个发请求，识别耗时约 100 秒；改成按需展开后**降到 35 秒**，
而且不会一次发出几十个请求（更容易触发 YouTube 风控）。

播放列表标签页的计数是**播放列表个数**（与原页面一致），不是里面的视频总数。

### 其他

- 修复 `pushItem` 去重分支不推进分类数组的 bug：会导致「视频确实属于某个分类、
  但该分类的标签页不显示」——界面是用分类数组的长度决定标签页显不显示的。
- 同一视频现在可以同时归属多个分类（例如既在 Videos 里、又属于某个播放列表）。
- 识别结果有 30 分钟缓存，同样的频道重复识别是秒开。

### 已知遗留问题（本次未修）

**Cookie 与代理设置只作用于下载，没有传给「识别」和「视频探测」。**
`channel.js` 的 `dumpFlat()` / `probeVideo()` 里没有 `--cookies` / `--proxy`，
所以设置里那句「Cookies 文件（会员 / 年龄限制 / 防机器人验证）」在**识别阶段是无效的**。
遇到风控时目前只能等待或更换网络（识别阶段 30 分钟缓存内不受影响）。

---

## v1.3.1 — 队列按钮精简（2026-09-11）

队列任务上原本有 6 个按钮（打开 / 音频 / 字幕 / 彩色字幕 / 文档 / 重做），
现在每个视频一个独立文件夹、所有产物都在里面，那些单文件快捷按钮已无必要。

**精简为「打开」+「重做」**：
- **打开** —— 打开该视频的文件夹，视频/音频/英文字幕/彩色双语字幕/学习文档全在里面
- **重做** —— 重新生成学习文档（走翻译缓存，不产生 API 费用；按住 Ctrl 强制重新翻译）

保留「✕ 从队列移除」：它不是文件快捷方式，而且正在排队/下载中的任务只有它一个按钮，去掉就没法移除了。

---

## v1.3.0 — 每个视频单独一个文件夹（2026-09-11）

下载时自动建一个**以命名模板渲染结果命名**的文件夹，该视频的所有产物都放进去：

```
F:\YouTube下载\World’s Largest Tennis Match [2026-08-23]\
  ├ World’s Largest Tennis Match [2026-08-23].mp4
  ├ World’s Largest Tennis Match [2026-08-23].mp3
  ├ World’s Largest Tennis Match [2026-08-23].en.srt
  ├ World’s Largest Tennis Match [2026-08-23].zh-en.ass
  └ World’s Largest Tennis Match [2026-08-23].学习文档.docx
```

### 实现方式

把文件名模板拼成**嵌套模板**交给 yt-dlp 的 `-o`——模板里带 `/` 时 yt-dlp 会自动创建目录：

```
%(title)s [%(upload_date>%Y-%m-%d)s].%(ext)s
  → %(title)s [%(upload_date>%Y-%m-%d)s]/%(title)s [%(upload_date>%Y-%m-%d)s].%(ext)s
```

关键点是**视频、字幕、ASS、Word 必须用同一个模板**，否则会散落在不同层级。
字幕步骤单独跑一次 yt-dlp，所以那处也要用转换后的模板；ASS 与 Word 的路径由视频路径推导，自然落在同一目录。

模板里没有 `%(ext)s` 时退化为「模板目录/%(title)s.%(ext)s」；设置里可关闭该行为（勾选项默认开启）。

### 说明

- 文件夹名与文件名**同名**（都来自模板），这样单独把文件拷出去也自带标识信息
- 旧版本下载的平铺文件**不会被自动移动**；如需整理可手动归入文件夹，或等后续版本加一个「整理到文件夹」的功能

---

## v1.2.0 — 中英不同色 ASS 双语字幕（2026-09-11）

生成 `标题 [日期].zh-en.ass`，**中英分别设色 + 黑描边**，播放器（PotPlayer / VLC / MPV）直接加载。

### 为什么必须用 ASS

SRT 是纯文本、**零样式能力**；MP4 内嵌字幕用的 `mov_text` 对样式支持极不一致。
只有 **ASS** 支持「逐行独立字体 / 颜色 / 描边 / 位置」——这是"中英不同色"的唯一途径。

### 实现要点（都是实测踩出来的）

1. **中英必须合并成同一个事件**，而不是两条独立事件各挂一个样式。
   libass 有**防重叠机制**：两个时间相同、位置交叠的块会被自动挪开，把精心设的 `MarginV` 彻底打乱。
   实测表现是"无论怎么调 MarginV，中文都跑到英文上面"。
   合并成一个事件后中英作为整体定位，两行的颜色/字体/字号差异改用行内标签 `{\r\c..\fn..\fs..}` 覆盖。

2. **ASS 颜色是 `&HAABBGGRR`**（透明度 + BGR 倒序），不是 RGB。界面上给用户的是 `#RRGGBB`，代码里转换。

3. **折行上限必须由「可用宽度 ÷ 字号」推导**，不能按固定字符数。
   实测竖屏 1080 宽 + 字号 89 时装不下 33 个字符，会被 libass 自动折行打乱排版。
   用户设置只作为「可读性上限」，与硬性宽度上限取较小值。

4. **中文折行要按显示宽度算**：一个拉丁字母约为一个汉字的 0.5 倍宽。
   按字符个数算会把 `Huberman Lab Essentials` 从中间切断（实测出现 `…Lab Esse\Nntials…`）。
   同时避免在英文单词内部断行，并优先在标点处断。

5. **分辨率自动适配**：字号/描边/边距以视频高度为基准等比缩放。
   横屏 1080p 字号 50、竖屏 Shorts（1080×1920）字号 89、4K 则 100——视觉大小一致。
   分辨率优先取下载时抓到的宽高，缺失时用 ffmpeg 探测兜底。

### 界面

- 设置里可调：英文字色 / 中文字色 / 描边色 / 描边粗细 / 字号缩放 / 中英每行字数上限
- 队列任务上新增「彩色字幕」按钮定位 ASS 文件
- **默认不再生成 `.zh-en.srt`**：两者并存时播放器加载哪个没有统一规则，
  容易"看到的是 srt 所以没有颜色"。关闭 srt 时会自动清理旧的 `.zh-en.srt`（该文件可从缓存免费重建）

### 验证方式

用内置 ffmpeg 的 **libass** 渲染实际帧做视觉验证（与播放器同一渲染引擎），
横屏 / 竖屏各验证一遍，确认颜色、描边、中英顺序、折行、分辨率缩放全部正确。

---

## v1.1.3 — 「仅显示未下载」过滤器（2026-09-11）

- **改用显示过滤器**：工具栏新增「仅显示未下载」勾选项，勾上后列表里**只显示还没下载过的视频**，
  已下载完成的会从列表中消失；某条下载完成时会自动从列表里移除。
  只影响「显示什么」，**不改动已有勾选**；当前分类全下完时会给出明确提示而不是留空列表。
- **撤掉 v1.1.1 加的「选中未下载」按钮**：与原有的「仅选未下载」含义太接近，容易混淆。
  勾选类操作仍由「全选 / 清空 / 反选 / 仅选未下载」四个按钮负责。

---

## v1.1.2 — 文档字体与元信息（2026-09-11）

- **Word 里所有英文（含音标）改用 Times New Roman**。音标里含 IPA 字符（`ˈ ə ʊ ː` 等），
  之前用 Georgia 会掉进回退字体、字距错乱；Times New Roman 的 IPA 覆盖完整，显示正常。
  中文仍用微软雅黑（`w:rFonts` 的 `eastAsia`），同一段里的中英文各走各的字体。
- **元信息里的「生成日期」改为「视频上传日期」**。上传日期在下载开始时由 yt-dlp 抓取
  （`--print before_dl:` 输出 `duration|upload_date|channel`），比生成时间对学习材料更有意义。
  旧任务没有该字段时，会从文件名的 `[YYYY-MM-DD]` 里兜底提取。

---

## v1.1.1 — 交互修补（2026-09-11）

- **新增「选中未下载」按钮**：在已选基础上**追加**所有未下载的视频（与「仅选未下载」的区别是不清空已有选择）
- **保存设置后自动关闭设置弹窗**，不用再手动点叉
- **Word 文档去掉「翻译模型」那一行**（元信息只保留频道/时长/链接/生成时间）
- **修复文档被占用时报错难懂**：若 Word 正开着该文档，点「重做」原先会抛出 `EBUSY: resource busy or locked`。现在会明确提示「文件被占用…请先关闭再重试」，并对磁盘空间不足（ENOSPC）也给了中文提示

> 已生成的旧文档仍带「翻译模型」行；在队列里对该任务点一次「重做」即可（走翻译缓存，**不产生 API 费用**）。

---

## v1.1.0 — 中英对照学习文档（2026-09-11）

在 v1.0.0 基础上新增：字幕下载完成后**自动调用大模型**，把字幕翻译成中英对照并排版成 Word 学习文档。

### 新增功能

**学习文档（大模型）**
- 字幕就绪后**自动**生成，无需手动触发（可在设置或底部勾选项关闭）
- 产出两份文件，与视频同名同目录：
  - `标题 [日期].学习文档.docx` —— 中英对照 + 长难句精讲 + 词汇 + 纯英文版 + 词汇总表
  - `标题 [日期].zh-en.srt` —— 中英双语字幕，播放器可直接加载
- Word 文档结构：标题与元信息（频道/时长/上传日期/链接/模型）→ 逐段「英文段 + 中文段 + 长难句精讲 + 重点词汇」→ 附录一纯英文版 → 附录二词汇总表（按出现段数排序的表格）；页眉放标题、页脚放页码
- 排版由代码确定性地渲染（docx 库），中英文字体分别指定，版式稳定可复现

**大模型接入**
- 统一走 **OpenAI 兼容协议**，填 Base URL + API Key + 模型名即可，DeepSeek / OpenAI / 通义 / Kimi / GLM / OpenRouter / 本地 Ollama 通用
- 设置里有**「测试连接」**按钮，直接验证 Key 与网络
- **API Key 用 Electron safeStorage（Windows DPAPI）加密存储**，且从不回传到界面（只回显打码串）
- 翻译**结果落盘缓存**（按 视频ID + 模型 + 提示词版本 作键）：改排版选项重新生成文档不再产生费用，切换模型或改提示词会自动失效

**成本可见与可控**
- 生成前按字幕规模**预估 token 与费用**，队列里实时累计本次花费
- 每段独立缓存，失败只重试该段，不重复付费
- 护栏配置项（最短时长 / 单视频费用上限 / 本批累计上限）已实现，**默认全部为 0 即不限制**

### 流水线的关键技术决策

这些是拿真实视频反复实测后定下来的，直接决定成品质量：

1. **滚动字幕去重**：YouTube 自动字幕每条会重复上一条的内容，实测 1602 条里有 800 条是重复（重复正文 28826 字符）。不去重直接送模型，token 白烧一倍以上。
2. **句末切分 cue**：一个 cue 里常装着「上一句结尾 + 下一句开头」，不切开的话分段边界必然切断句子（实测 17/24 段出现残句）。先按句子切开 cue，再把分段边界吸附到句末，残句降到 **0/36**。
3. **两遍法**：第一遍只看结构产出分段计划（输出小、成本低），第二遍按段精加工。每段独立可缓存、可重试、可并行。
4. **大段主动切小**：50+ 条的大段模型容易漏条目，触发昂贵的整段重发。提前切到 45 条以内。
5. **逐条中文用行分隔而非 JSON 数组**：用 `{"i":349,"zh":"..."}` 时模型会合并同句的条目（所有失败只有「缺」没有「多」，是合并的典型特征）。改成 `349|中文` 后物理上无法合并，重试次数下降、输出 token 省约 24%。
6. **允许修正明显的语音识别错误**：实测模型会自行把人名、专业术语的拼写改对（`opthalmology` → `ophthalmology`、`Gino` → `Gina`）。

### 开发期发现并修复的问题

- **打包配置会把 docx 排除**：`files` 里的 `!**/node_modules/**/*` 会让运行时依赖不进包，打包版功能直接失效。已改为交给 electron-builder 自动处理生产依赖。
- **字幕请求 429 拖垮整个任务**：见 v1.0.0 条目 5，本版进一步把「部分语言失败」也视为成功。
- **`--convert-subs` 属后处理**：某语言失败导致中途退出时会跳过转换，只留下 `.vtt`。已加本地 ffmpeg 兜底转 `.srt`。

### 测试

- 集成测试仍 **75/75** 通过（新增 `studyDoc:false` 开关使测试不触发大模型）
- `tools/study-cli.js`：不启动 Electron 直接跑完整链路，用于开发验证与提示词调优
- `tools/translate-probe.js`：只验证「清洗 → 分段 → 翻译」质量，支持 `--dry-run` 零成本预览清洗结果、`--ping` 测连通性
- 生成出的 Word 文档已用**真实 Microsoft Word** 打开验证：75 页 / 1251 段 / 132 行词汇表 / 页眉页脚正确

### 实测成本

29 分钟、5402 词的英文视频（1013 条清洗后字幕）：**输入约 52k token + 输出约 55k token ≈ 两三毛钱**，耗时约 280 秒。

---

## v1.0.0 — 首个正式版（2026-09-11）

Windows 桌面版 YouTube 频道批量下载器。粘贴博主主页，自动识别 **Videos / Shorts / Live 直播 / Podcasts** 全部内容并批量下载。

### 功能

**识别与列表**
- 支持 `@handle`、`youtube.com/@handle`、`/videos` 等标签页链接、`/channel/UC...`、单个视频、播放列表；域名自动归一化
- 四分类抓取，Live 细分「正在直播 / 即将开播 / 往期回放」
- Podcasts 标签页的「播放列表容器」自动二级展开为单集
- 频道无某标签时明确提示（区分「没有该标签」与「真的出错」）
- 跨分类去重：同一视频只出现一次
- 搜索筛选、全选/反选/仅选未下载、滚动增量加载
- 30 分钟频道缓存，可强制重新抓取

**下载**
- 画质：最佳 / 2160p / 1440p / 1080p / 720p / 480p / 360p
- 视频编码策略：画质优先（最佳编码 + AAC）／兼容优先（H.264 + AAC）
- **视频必定带原声**：音频强制 AAC 封装进 MP4
- **同时下载音频（默认开启）**：本地 ffmpeg 抽轨导出同名 MP3，不重复下载
- **字幕下载（默认开启）**：原生语言字幕（默认 `en`），SRT/VTT/ASS 可选，失败不影响视频
- 仅音频 MP3 模式
- 文件名模板默认 `标题 [上传日期].扩展名`
- 限速、代理、Cookies 文件（会员/年龄限制内容）
- 直播从头录制（需该直播支持 DVR）

**队列**
- 并发数可调（1–6）、暂停/继续/重试/移除、全部暂停/继续、清除已完成
- 实时进度、速度、剩余时间
- **断点续传三层保障**：yt-dlp 字节级 `.part` 续传 + 队列状态落盘跨重启继续 + `--download-archive` 去重
- 取消/退出用 `taskkill /T` 杀进程树，绝不留孤儿下载进程

**系统**
- 内置 yt-dlp.exe + ffmpeg.exe，用户零依赖
- 内核一键更新（下载到用户目录，无需管理员权限），可恢复内置版本
- 默认下载目录 `F:\YouTube下载`，并有「F → 其他非 C 盘 → 系统视频目录」降级链与盘符自愈
- NSIS 安装包 + 免安装版

### 开发期发现并修复的关键缺陷

按严重程度：

1. **成品视频没有声音** — YouTube 高画质下给 AV1/VP9 + Opus，而 `--merge-output-format mp4` 会把 Opus 塞进 MP4（非标准组合），大多数播放器解不出音频轨。修复：格式串强制优先取 m4a/AAC（`bv*+ba[ext=m4a]/bv*+ba/b`），只改音频选择因而已下载的分片仍可续传。
2. **界面全黑** — `contextBridge` 暴露的 `window.api` 是全局对象上的不可配置属性，渲染脚本里写 `const api = window.api` 直接抛 SyntaxError，整个脚本不执行。修复：整段包进 IIFE。
3. **非 ASCII 文件名/路径乱码** — yt-dlp 纯文本输出是系统 ANSI 代码页（中文 Windows 为 GBK），按 UTF-8 解码会得到 `U+FFFD`。修复：所有调用加 `--encoding utf-8`，并按字节切行、整行再解码。
4. **续传时整单被误判为「已存在，跳过」** — 断点续传时 yt-dlp 会为已完成的那一路流打印 `has already been downloaded`，据此判定整单跳过会把「实际下载了几十 MB 并成功产出成品」误报成跳过。修复：只有本轮零字节才算跳过。
5. **字幕限流拖垮整个任务** — 字幕请求遇 HTTP 429 时视频明明已下好却被标记失败。修复：字幕拆为独立步骤，失败只记录不影响成品。
6. **进度条恒为 0** — `--print` 隐含 `--quiet` 和 `--simulate`。修复：加 `--no-quiet --no-simulate`。
7. **没有直播标签的频道整个 Live 分类消失** — `tabStatus` 用 `streams` 作键而渲染层按 `live` 查。修复：统一为分类键。
8. **孤儿下载进程** — 只 `child.kill()` 会留下继续后台下载的 yt-dlp。修复：`taskkill /pid <pid> /T /F` 杀进程树。
9. **字幕格式不一致** — `--convert-subs` 属后处理，某语言失败导致中途退出时被跳过，只留 `.vtt`。修复：本地 ffmpeg 兜底转 `.srt`。
10. **磁盘/路径自愈缺失** — 默认目录硬编码盘符在别的机器上会崩。修复：降级链 + 启动时自愈不存在的盘符。

### 测试

- `tools/integration-test.js`：75+ 项联网集成测试（链接解析、真实频道枚举、MP3 提取、暂停/断点续传、部分流已存在回归、成品音频编码与可解码性、文件名模板、字幕下载、错误处理、编码乱码回归、默认目录不在 C 盘）
- `tools/ui-scenario-*.js`：用真实 DOM 事件驱动完整 UI 流程
- `tools/capture-window.ps1`：不受窗口遮挡影响的窗口截图工具
- 支持 `YTLD_QUICK=1` 快速模式

### 已知限制

- 代码未签名，首次运行会触发 Windows SmartScreen 提示
- 正在直播需挂着录完；尚未开播的直播会被拒绝
- 大频道首次全量枚举需 20–60 秒
- 会员/年龄限制内容需自备 `cookies.txt`
- YouTube 对「自动翻译字幕轨」限流很严（英文视频求 zh-Hans 常返回 HTTP 429），故默认只取原生语言字幕
- 安装包约 148 MB（Electron 运行时 + ffmpeg）
