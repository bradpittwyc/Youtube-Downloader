# 更新日志

## v1.1.3 — 「仅显示未下载」过滤器（2026-09-11）

- **改用显示过滤器**：工具栏新增「仅显示未下载」勾选项，勾上后列表里**只显示还没下载过的视频**，
  已下载完成的会从列表中消失；某条下载完成时会自动从列表里移除。
  只影响「显示什么」，**不改动已有勾选**；当前分类全下完时会给出明确提示而不是留空列表。
- **撤掉 v1.1.1 加的「选中未下载」按钮**：与原有的「仅选未下载」含义太接近，容易混淆。
  勾选类操作仍由「全选 / 清空 / 反选 / 仅选未下载」四个按钮负责。

---

## v1.1.2 — 文档字体与元信息（2026-09-11）

- **Word 里所有英文（含音标）改用 Times New Roman**。音标里含 IPA 字符（`ˈ ə ʊ ː` 等），
  之前用 Georgia 会掉进回退字体、字距错乱；Times New Roman 的 IPA 覆盖完整，显示正常。
  中文仍用微软雅黑（`w:rFonts` 的 `eastAsia`），同一段里的中英文各走各的字体。
- **元信息里的「生成日期」改为「视频上传日期」**。上传日期在下载开始时由 yt-dlp 抓取
  （`--print before_dl:` 输出 `duration|upload_date|channel`），比生成时间对学习材料更有意义。
  旧任务没有该字段时，会从文件名的 `[YYYY-MM-DD]` 里兜底提取。

---

## v1.1.1 — 交互修补（2026-09-11）

- **新增「选中未下载」按钮**：在已选基础上**追加**所有未下载的视频（与「仅选未下载」的区别是不清空已有选择）
- **保存设置后自动关闭设置弹窗**，不用再手动点叉
- **Word 文档去掉「翻译模型」那一行**（元信息只保留频道/时长/链接/生成时间）
- **修复文档被占用时报错难懂**：若 Word 正开着该文档，点「重做」原先会抛出 `EBUSY: resource busy or locked`。现在会明确提示「文件被占用…请先关闭再重试」，并对磁盘空间不足（ENOSPC）也给了中文提示

> 已生成的旧文档仍带「翻译模型」行；在队列里对该任务点一次「重做」即可（走翻译缓存，**不产生 API 费用**）。

---

## v1.1.0 — 中英对照学习文档（2026-09-11）

在 v1.0.0 基础上新增：字幕下载完成后**自动调用大模型**，把字幕翻译成中英对照并排版成 Word 学习文档。

### 新增功能

**学习文档（大模型）**
- 字幕就绪后**自动**生成，无需手动触发（可在设置或底部勾选项关闭）
- 产出两份文件，与视频同名同目录：
  - `标题 [日期].学习文档.docx` —— 中英对照 + 长难句精讲 + 词汇 + 纯英文版 + 词汇总表
  - `标题 [日期].zh-en.srt` —— 中英双语字幕，播放器可直接加载
- Word 文档结构：标题与元信息（频道/时长/上传日期/链接/模型）→ 逐段「英文段 + 中文段 + 长难句精讲 + 重点词汇」→ 附录一纯英文版 → 附录二词汇总表（按出现段数排序的表格）；页眉放标题、页脚放页码
- 排版由代码确定性地渲染（docx 库），中英文字体分别指定，版式稳定可复现

**大模型接入**
- 统一走 **OpenAI 兼容协议**，填 Base URL + API Key + 模型名即可，DeepSeek / OpenAI / 通义 / Kimi / GLM / OpenRouter / 本地 Ollama 通用
- 设置里有**「测试连接」**按钮，直接验证 Key 与网络
- **API Key 用 Electron safeStorage（Windows DPAPI）加密存储**，且从不回传到界面（只回显打码串）
- 翻译**结果落盘缓存**（按 视频ID + 模型 + 提示词版本 作键）：改排版选项重新生成文档不再产生费用，切换模型或改提示词会自动失效

**成本可见与可控**
- 生成前按字幕规模**预估 token 与费用**，队列里实时累计本次花费
- 每段独立缓存，失败只重试该段，不重复付费
- 护栏配置项（最短时长 / 单视频费用上限 / 本批累计上限）已实现，**默认全部为 0 即不限制**

### 流水线的关键技术决策

这些是拿真实视频反复实测后定下来的，直接决定成品质量：

1. **滚动字幕去重**：YouTube 自动字幕每条会重复上一条的内容，实测 1602 条里有 800 条是重复（重复正文 28826 字符）。不去重直接送模型，token 白烧一倍以上。
2. **句末切分 cue**：一个 cue 里常装着「上一句结尾 + 下一句开头」，不切开的话分段边界必然切断句子（实测 17/24 段出现残句）。先按句子切开 cue，再把分段边界吸附到句末，残句降到 **0/36**。
3. **两遍法**：第一遍只看结构产出分段计划（输出小、成本低），第二遍按段精加工。每段独立可缓存、可重试、可并行。
4. **大段主动切小**：50+ 条的大段模型容易漏条目，触发昂贵的整段重发。提前切到 45 条以内。
5. **逐条中文用行分隔而非 JSON 数组**：用 `{"i":349,"zh":"..."}` 时模型会合并同句的条目（所有失败只有「缺」没有「多」，是合并的典型特征）。改成 `349|中文` 后物理上无法合并，重试次数下降、输出 token 省约 24%。
6. **允许修正明显的语音识别错误**：实测模型会自行把人名、专业术语的拼写改对（`opthalmology` → `ophthalmology`、`Gino` → `Gina`）。

### 开发期发现并修复的问题

- **打包配置会把 docx 排除**：`files` 里的 `!**/node_modules/**/*` 会让运行时依赖不进包，打包版功能直接失效。已改为交给 electron-builder 自动处理生产依赖。
- **字幕请求 429 拖垮整个任务**：见 v1.0.0 条目 5，本版进一步把「部分语言失败」也视为成功。
- **`--convert-subs` 属后处理**：某语言失败导致中途退出时会跳过转换，只留下 `.vtt`。已加本地 ffmpeg 兜底转 `.srt`。

### 测试

- 集成测试仍 **75/75** 通过（新增 `studyDoc:false` 开关使测试不触发大模型）
- `tools/study-cli.js`：不启动 Electron 直接跑完整链路，用于开发验证与提示词调优
- `tools/translate-probe.js`：只验证「清洗 → 分段 → 翻译」质量，支持 `--dry-run` 零成本预览清洗结果、`--ping` 测连通性
- 生成出的 Word 文档已用**真实 Microsoft Word** 打开验证：75 页 / 1251 段 / 132 行词汇表 / 页眉页脚正确

### 实测成本

29 分钟、5402 词的英文视频（1013 条清洗后字幕）：**输入约 52k token + 输出约 55k token ≈ 两三毛钱**，耗时约 280 秒。

---

## v1.0.0 — 首个正式版（2026-09-11）

Windows 桌面版 YouTube 频道批量下载器。粘贴博主主页，自动识别 **Videos / Shorts / Live 直播 / Podcasts** 全部内容并批量下载。

### 功能

**识别与列表**
- 支持 `@handle`、`youtube.com/@handle`、`/videos` 等标签页链接、`/channel/UC...`、单个视频、播放列表；域名自动归一化
- 四分类抓取，Live 细分「正在直播 / 即将开播 / 往期回放」
- Podcasts 标签页的「播放列表容器」自动二级展开为单集
- 频道无某标签时明确提示（区分「没有该标签」与「真的出错」）
- 跨分类去重：同一视频只出现一次
- 搜索筛选、全选/反选/仅选未下载、滚动增量加载
- 30 分钟频道缓存，可强制重新抓取

**下载**
- 画质：最佳 / 2160p / 1440p / 1080p / 720p / 480p / 360p
- 视频编码策略：画质优先（最佳编码 + AAC）／兼容优先（H.264 + AAC）
- **视频必定带原声**：音频强制 AAC 封装进 MP4
- **同时下载音频（默认开启）**：本地 ffmpeg 抽轨导出同名 MP3，不重复下载
- **字幕下载（默认开启）**：原生语言字幕（默认 `en`），SRT/VTT/ASS 可选，失败不影响视频
- 仅音频 MP3 模式
- 文件名模板默认 `标题 [上传日期].扩展名`
- 限速、代理、Cookies 文件（会员/年龄限制内容）
- 直播从头录制（需该直播支持 DVR）

**队列**
- 并发数可调（1–6）、暂停/继续/重试/移除、全部暂停/继续、清除已完成
- 实时进度、速度、剩余时间
- **断点续传三层保障**：yt-dlp 字节级 `.part` 续传 + 队列状态落盘跨重启继续 + `--download-archive` 去重
- 取消/退出用 `taskkill /T` 杀进程树，绝不留孤儿下载进程

**系统**
- 内置 yt-dlp.exe + ffmpeg.exe，用户零依赖
- 内核一键更新（下载到用户目录，无需管理员权限），可恢复内置版本
- 默认下载目录 `F:\YouTube下载`，并有「F → 其他非 C 盘 → 系统视频目录」降级链与盘符自愈
- NSIS 安装包 + 免安装版

### 开发期发现并修复的关键缺陷

按严重程度：

1. **成品视频没有声音** — YouTube 高画质下给 AV1/VP9 + Opus，而 `--merge-output-format mp4` 会把 Opus 塞进 MP4（非标准组合），大多数播放器解不出音频轨。修复：格式串强制优先取 m4a/AAC（`bv*+ba[ext=m4a]/bv*+ba/b`），只改音频选择因而已下载的分片仍可续传。
2. **界面全黑** — `contextBridge` 暴露的 `window.api` 是全局对象上的不可配置属性，渲染脚本里写 `const api = window.api` 直接抛 SyntaxError，整个脚本不执行。修复：整段包进 IIFE。
3. **非 ASCII 文件名/路径乱码** — yt-dlp 纯文本输出是系统 ANSI 代码页（中文 Windows 为 GBK），按 UTF-8 解码会得到 `U+FFFD`。修复：所有调用加 `--encoding utf-8`，并按字节切行、整行再解码。
4. **续传时整单被误判为「已存在，跳过」** — 断点续传时 yt-dlp 会为已完成的那一路流打印 `has already been downloaded`，据此判定整单跳过会把「实际下载了几十 MB 并成功产出成品」误报成跳过。修复：只有本轮零字节才算跳过。
5. **字幕限流拖垮整个任务** — 字幕请求遇 HTTP 429 时视频明明已下好却被标记失败。修复：字幕拆为独立步骤，失败只记录不影响成品。
6. **进度条恒为 0** — `--print` 隐含 `--quiet` 和 `--simulate`。修复：加 `--no-quiet --no-simulate`。
7. **没有直播标签的频道整个 Live 分类消失** — `tabStatus` 用 `streams` 作键而渲染层按 `live` 查。修复：统一为分类键。
8. **孤儿下载进程** — 只 `child.kill()` 会留下继续后台下载的 yt-dlp。修复：`taskkill /pid <pid> /T /F` 杀进程树。
9. **字幕格式不一致** — `--convert-subs` 属后处理，某语言失败导致中途退出时被跳过，只留 `.vtt`。修复：本地 ffmpeg 兜底转 `.srt`。
10. **磁盘/路径自愈缺失** — 默认目录硬编码盘符在别的机器上会崩。修复：降级链 + 启动时自愈不存在的盘符。

### 测试

- `tools/integration-test.js`：75+ 项联网集成测试（链接解析、真实频道枚举、MP3 提取、暂停/断点续传、部分流已存在回归、成品音频编码与可解码性、文件名模板、字幕下载、错误处理、编码乱码回归、默认目录不在 C 盘）
- `tools/ui-scenario-*.js`：用真实 DOM 事件驱动完整 UI 流程
- `tools/capture-window.ps1`：不受窗口遮挡影响的窗口截图工具
- 支持 `YTLD_QUICK=1` 快速模式

### 已知限制

- 代码未签名，首次运行会触发 Windows SmartScreen 提示
- 正在直播需挂着录完；尚未开播的直播会被拒绝
- 大频道首次全量枚举需 20–60 秒
- 会员/年龄限制内容需自备 `cookies.txt`
- YouTube 对「自动翻译字幕轨」限流很严（英文视频求 zh-Hans 常返回 HTTP 429），故默认只取原生语言字幕
- 安装包约 148 MB（Electron 运行时 + ffmpeg）
